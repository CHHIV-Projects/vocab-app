```text
MILESTONE M004.4
BOUNDED GPT-OSS LEXICAL SYNTHESIS + DETERMINISTIC VALIDATION

ROLE / WORKING MODEL

You are the coding agent for the Vocab App.

Product Owner: Chuck
Architect: ChatGPT
Canonical repository:
    /home/chuck/projects/vocab-app

Canonical branch:
    main

Canonical GitHub:
    CHHIV-Projects/vocab-app

Use High reasoning for this milestone.

This milestone has TWO CONTROLLED PHASES:

    Phase A — reconnaissance only
    Phase B — implementation

Phase A is a mandatory stop/checkpoint.

Do not begin Phase B implementation until the Product Owner explicitly
authorizes continuation after reviewing the reconnaissance report.

Do not commit, tag, push, merge, or modify unrelated repositories unless explicitly instructed after Product Owner validation.

Vocab App and Repo Control are separate repositories. Do not modify:
    /home/chuck/projects/repo-control

Repo Control may be used for deterministic repository-state checks, snapshots, comparisons, and narrow symbol/file lookup where useful. It is not an architectural reasoning substitute. Direct source inspection and Git remain authoritative when needed.


======================================================================
1. MILESTONE OBJECTIVE
======================================================================

Implement the first bounded local-AI lexical synthesis layer on top of the production lexical evidence system completed through M004.3.1.

The architecture is:

    Wiktionary / Wiktextract
              +
           WordNet
              |
              v
      Canonical Evidence
              |
              v
    deterministic evidence packer
              |
              v
        GPT-OSS:20b
     bounded synthesis only
              |
              v
    deterministic validator
              |
              v
      validated synthesis

The central architectural rule is:

    Canonical evidence is lexical authority.
    GPT-OSS is not lexical authority.

GPT-OSS may organize, consolidate, summarize, paraphrase, and prioritize supplied evidence for learner use, but it may not fill missing lexical facts from model memory.

The validator is the gatekeeper for all objective/mechanical requirements.

If validation fails:

    FAIL IS FAIL.

There is:
    - no automatic repair;
    - no hidden retry;
    - no automatic resubmission;
    - no attempt to salvage the candidate.

The failure must be recorded with sufficient diagnostic state for root-cause analysis.

A later user-initiated new query may generate a fresh inference attempt, but that is not part of automatic failure recovery.


======================================================================
2. CONTROLLING PRIOR WORK
======================================================================

Before coding, inspect the existing implementation and milestone closeouts for:

    M004.2  Lexical Evidence Architecture
    M004.3  Deterministic Lexical Engine
    M004.3.1 First Production Wiktionary Dataset Build

Treat those artifacts and the current code as controlling implementation context.

Important current production lexical facts include:

Lexical root:
    /home/chuck/.local/share/vocab-lexical

Active-version pointer:
    /home/chuck/.local/share/vocab-lexical/active

Current production version:
    wiktionary-20260901

Production SQLite:
    /home/chuck/.local/share/vocab-lexical/versions/
        wiktionary-20260901/lexical.sqlite

Production evidence JSONL:
    evidence.jsonl

Production dataset scale:
    normalized records: ~1.586 million
    SQLite senses: ~1.895 million

Representative production complexity:

    archipelago:
        2 entries
        6 senses

    run:
        5 entries
        119 Wiktionary senses
        57 WordNet synsets

    running:
        6 entries
        20 senses
        52 WordNet synsets

Current exact lookup behavior intentionally does not silently normalize
"running" into "run".

Current complex-word lookup performance for "run" has been approximately
4-5+ seconds and must be measured again as part of this milestone.

Do not alter the immutable/versioned canonical lexical architecture unless a genuine blocker is found.


======================================================================
3. PRE-FLIGHT
======================================================================

Before implementation, capture and report:

    pwd
    git branch --show-current
    git status --short
    git rev-parse HEAD
    git rev-parse origin/main

Confirm:

    repository == /home/chuck/projects/vocab-app
    branch == main

Inspect the relevant lexical modules, tests, manifests, milestone closeouts,
and current runtime configuration.

Do not perform broad repository reconnaissance unrelated to this milestone.


======================================================================
4. HARD SCOPE BOUNDARIES
======================================================================

IN SCOPE:

1. Local GPT-OSS:20b runtime reconnaissance.
2. A bounded model-provider interface.
3. Deterministic evidence packaging.
4. Deterministic size/token budgeting and chunking.
5. Structured GPT lexical synthesis.
6. Multi-stage synthesis for large/polysemous words.
7. Deterministic validation.
8. Validated-result caching.
9. System-failure diagnostics.
10. A CLI/test harness sufficient for live Product Owner validation.
11. Tests and performance measurements.
12. Milestone closeout documentation.

OUT OF SCOPE:

- learner-facing UI changes;
- Streamlit redesign;
- Save button implementation;
- Retry button UI;
- Refresh button UI;
- Flag button UI;
- PostgreSQL accepted-entry/version schema changes;
- PostgreSQL learner-history changes;
- translation work;
- replacement of gTTS;
- automated lexical dataset update scheduling;
- six-month update automation;
- GPT-generated lexical facts;
- GPT-generated example sentences;
- automatic correction/repair of validator failures;
- broad infrastructure redesign;
- model installation/upgrades without Product Owner approval.

M004.5 will handle learner-facing UI and PostgreSQL save/versioning.

Do not expand this milestone into M004.5.


======================================================================
5. LOCAL GPT-OSS RUNTIME RECONNAISSANCE
======================================================================

Determine how the existing local GPT-OSS:20b model is actually being served on henderson-server1.

Do not assume Ollama, Open WebUI, a specific port, or a specific API contract.

Inspect the existing local runtime only far enough to determine:

- serving mechanism;
- model identifier;
- model digest/version where available;
- API/interface available to Vocab App;
- context-window configuration;
- structured-output / JSON capabilities;
- token-counting capability if exposed;
- seed control if exposed;
- temperature/sampling controls;
- timeout behavior;
- GPU use;
- practical memory behavior;
- practical latency.

Also inspect the existing lexical implementation far enough to determine:

- exact production evidence-bundle shape presented by the current lexical engine;
- source-entry / POS / etymology / sense ordering available to the packer;
- stable evidence and sense identities already available;
- existing token/serialization helpers, if any;
- current evidence_set_hash generation path;
- current complex-word retrieval cost;
- whether production lookup performs unnecessary work that would materially affect synthesis;
- suitable existing boundaries for provider, packer, validator, cache, and diagnostics;
- existing runtime-data conventions suitable for cache and failure diagnostics.

Do not reconfigure unrelated local-AI infrastructure merely to satisfy this milestone.

If the currently installed service cannot be used safely through a bounded application interface, report the blocker rather than redesigning the server.


======================================================================
5A. MANDATORY RECONNAISSANCE-ONLY CHECKPOINT
======================================================================

THIS IS A HARD STOP.

Complete Sections 2, 3, and 5 as reconnaissance only.

Before this checkpoint, you MAY:

- inspect repository files;
- inspect milestone closeouts;
- inspect configuration;
- inspect existing runtime services;
- inspect the active lexical dataset and manifests;
- run bounded read-only lexical probes;
- run bounded read-only model-runtime/API probes;
- measure existing lookup/model behavior;
- inspect installed package/runtime versions;
- inspect logs/status where read-only;
- use temporary shell/Python probes that do not modify repository/runtime state;
- run existing tests if useful and non-mutating.

Before this checkpoint, you MUST NOT:

- edit repository source files;
- add implementation files;
- change schemas;
- change runtime configuration;
- install or upgrade models/packages;
- modify Docker/Compose configuration;
- create the synthesis cache implementation;
- create the diagnostics implementation;
- implement the provider adapter;
- implement the evidence packer;
- implement chunking;
- implement prompts;
- implement validator logic;
- modify tests as implementation work;
- modify UI;
- modify PostgreSQL;
- commit, push, tag, or merge.

The purpose of this checkpoint is to validate the actual implementation
environment before architecture is converted into code.

At the checkpoint, return a concise but complete:

    M004.4 RECONNAISSANCE REPORT

The report must include:

1. Git/repository preflight
   - repository
   - branch
   - HEAD
   - origin/main
   - working-tree status

2. Relevant current code architecture
   - lexical engine modules and boundaries
   - evidence bundle construction
   - evidence IDs / sense IDs
   - evidence_set_hash path
   - likely integration points
   - any existing helpers that should be reused

3. Production lexical evidence observations
   - representative object/record structure
   - POS / entry / etymology / sense ordering available
   - WordNet representation
   - pronunciation/form/example/relation data relevant to synthesis
   - any evidence gaps affecting the locked product requirements

4. GPT-OSS runtime
   - serving mechanism
   - model identity/version/digest where available
   - endpoint/interface
   - context-window setting
   - structured JSON support
   - token-count support
   - seed support
   - temperature/sampling controls
   - timeout behavior
   - local-only/network exposure status

5. Bounded runtime probes
   - simple prompt/API success
   - structured-output behavior
   - observed latency
   - GPU/RAM observations where practical
   - any obvious context-size or stability limits

6. Existing lexical lookup performance
   - at minimum archipelago and run
   - relevant timing breakdown if identifiable

7. Proposed token-budget method
   - exact tokenizer/runtime counter if available
   - otherwise proposed deterministic conservative estimate
   - known context allowance
   - proposed reserved output/prompt/headroom approach

Do NOT lock final numerical chunk budgets yet unless the reconnaissance
evidence is sufficient to justify them. If additional implementation-phase
calibration is required, say so.

8. Proposed implementation shape
   - provider boundary
   - evidence packer
   - synthesis service
   - validator
   - cache
   - diagnostics
   - CLI/live probe path
   - files likely to be added/modified

9. Proposed small-word / large-word processing path.

10. Known risks or architectural mismatches.

11. Any STOP/ESCALATION condition already encountered.

12. Recommended Phase B plan
    - smallest safe implementation sequence
    - focused tests
    - production probes
    - performance measurements

13. Explicit statement:

       NO IMPLEMENTATION CHANGES WERE MADE DURING RECONNAISSANCE

At this checkpoint:

    STOP.

Do not continue automatically into implementation.

Wait for explicit Product Owner authorization to proceed with Phase B.

If the Product Owner provides revised architectural direction in response to
the reconnaissance findings, incorporate that direction before implementation.

This checkpoint overrides any general instruction elsewhere in this prompt
that might otherwise imply reconnaissance and implementation should proceed
in one uninterrupted run.


======================================================================
6. MODEL AUTHORITY BOUNDARY
======================================================================

AFTER PRODUCT OWNER AUTHORIZATION TO PROCEED:

Every production GPT lexical prompt must make the authority boundary explicit.

The model must be told, in substance:

    - supplied evidence is the sole lexical authority;
    - do not use model memory to add meanings;
    - do not invent definitions;
    - do not invent synonyms;
    - do not invent etymologies;
    - do not invent forms;
    - do not invent pronunciations;
    - do not invent usage labels;
    - do not invent base/root relationships;
    - do not invent examples;
    - uncertainty or absence in the evidence must remain uncertainty or absence;
    - all lexical claims requiring evidence must reference supplied evidence IDs.

Prompt/schema/policy versions must be explicit and versioned in code.

At minimum establish version identities for:

    synthesis prompt/policy
    synthesis schema
    evidence packer
    validator

These identities must participate in diagnostics and caching where relevant.


======================================================================
7. DETERMINISTIC EVIDENCE PACKER
======================================================================

Do not send an unrestricted raw lexical record to GPT-OSS.

Implement a deterministic evidence-packing layer between the canonical lexical engine and GPT.

The packer must organize canonical evidence using source structure, not AI judgment.

Partition hierarchy:

    normalized queried lemma
        |
        +-- source
        |     Wiktionary
        |     WordNet
        |
        +-- lexical entry
        |
        +-- part of speech
        |
        +-- etymology grouping where applicable
        |
        +-- source-ordered senses
        |
        +-- bounded contiguous chunks if needed

Chunk boundaries must NEVER be:

    - random;
    - AI-selected;
    - popularity-selected;
    - semantic-clustering-selected.

Within the same lexical-entry / POS / etymology group:

    preserve source sense order.

Then fill each chunk sequentially until adding the next complete sense would exceed the configured target input budget.

Conceptually:

    sense 1
    sense 2
    sense 3
    ...
        |
        v
    add next complete sense if package remains within target
        |
        +-- yes -> continue
        |
        +-- no  -> close chunk
                   start next chunk with that sense

Never split a single sense in the middle merely to fill a token quota.

If one individual source sense itself exceeds the normal target, handle that deterministically and safely, document the exception, and never silently truncate evidence.


======================================================================
8. TOKEN / SIZE BUDGET
======================================================================

The chunk limit is not an arbitrary "N senses per chunk."

Derive a tested safe model-input budget.

Determine:

    practical model context capacity
    minus system/prompt instructions
    minus structured-output allowance
    minus safety headroom
    =
    safe evidence-input ceiling

Then choose a target package size materially below the hard ceiling.

Do not operate continuously at the model's theoretical maximum context size.

Prefer the exact model tokenizer or runtime token-count mechanism if locally available.

If exact preflight token counting is unavailable:

- implement a deterministic conservative estimate;
- document the method;
- calibrate it against observed runtime behavior;
- leave substantial safety margin.

Budget values must be configuration/constants rather than scattered magic numbers.

The architecture must allow a future model to use a different budget without changing lexical semantics.


======================================================================
9. COMPLETE-EVIDENCE RULE
======================================================================

Chunking is a processing mechanism only.

It must not become a filtering mechanism.

The complete relevant canonical evidence remains available to the synthesis pipeline even if processing requires multiple chunks.

No source sense may disappear because the word is too complex.

The system must maintain explicit coverage/accounting of source sense IDs through the entire pipeline.


======================================================================
10. SMALL-WORD VS LARGE-WORD PATH
======================================================================

Use the simplest safe path.

If the complete relevant synthesis evidence fits comfortably in one bounded package:

    evidence
        ->
    one final synthesis call
        ->
    validator

Do not force unnecessary multi-stage calls.

If the lexical record exceeds the safe package budget:

    canonical evidence
        |
        v
    deterministic chunks
        |
        v
    bounded chunk synthesis
        |
        v
    validate each chunk result
        |
        v
    deterministic intermediate assembly
        |
        v
    bounded final GPT organization
        |
        v
    final deterministic validation

No chunk-output validation failure may be automatically repaired.

If any required stage fails validation, the overall attempt fails and is logged.


======================================================================
11. PART-OF-SPEECH RULE
======================================================================

Part of speech is a hard semantic and presentation boundary.

Final learner meanings may be consolidated within the same POS where appropriate.

Do not consolidate meanings across different parts of speech.

Example:

    VERB
        meanings...

    NOUN
        meanings...

    ADJECTIVE
        meanings...

Uncommon, historical, technical, or archaic POS sections remain available if supported by canonical evidence.


======================================================================
12. CORE VS ADDITIONAL MEANINGS
======================================================================

The learner-facing synthesis should be concise by default but complete underneath.

Per POS, GPT-OSS should generally identify approximately:

    3-5 core learner-relevant senses

Use fewer when the word genuinely has fewer distinct supported meanings.

This is a presentation target, not a factual cap.

Core-sense selection policy:

Prefer:

- common contemporary meanings;
- meanings likely encountered in ordinary reading/speech;
- materially distinct meanings;
- representative major uses of the POS.

But:

- do not invent frequency statistics;
- do not claim corpus popularity unless supplied evidence supports it;
- do not assume Wiktionary source order is a formal popularity ranking;
- do not discard uncommon meanings.

All remaining distinct validated meanings must remain in:

    additional meanings

M004.5 will expose those through a "Show all meanings" style learner interface.

M004.4 must produce a complete structure that makes that future UI possible.


======================================================================
13. DUPLICATE / NEAR-DUPLICATE CONSOLIDATION
======================================================================

Compression is allowed.

Disappearance is not.

Exact duplicates that can be proven mechanically may be collapsed deterministically.

Near-duplicate meanings require bounded GPT semantic judgment.

If several source senses support the same learner-facing meaning, GPT may consolidate them.

Example:

    source sense A
    source sense B
    source sense C
          |
          v
    learner meaning X

But learner meaning X must retain all supporting source sense IDs.

Do not merge materially distinct meanings merely to reduce count.


======================================================================
14. SOURCE COVERAGE CONTRACT
======================================================================

Every source sense supplied to synthesis must be explicitly accounted for.

At final validation:

    all supplied source sense IDs
    minus
    all accounted-for source sense IDs

must equal:

    empty set

A source sense may be:

    - represented by a core meaning;
    - represented by an additional meaning;
    - consolidated into another supported learner meaning.

There may not be an unexplained:

    source sense -> nothing

Likewise:

    every learner-facing meaning must reference one or more valid supplied source sense IDs.

This is an objective deterministic validation requirement.


======================================================================
15. DEFINITION REWRITING
======================================================================

GPT-OSS may rewrite or combine source-supported definitions into clearer learner language.

It may:

- simplify;
- paraphrase;
- remove unnecessary dictionary jargon;
- combine genuinely overlapping supported meanings.

It may not:

- broaden meaning beyond supplied evidence;
- narrow meaning in a way that changes the source concept;
- add factual claims from model memory;
- convert an unsupported implication into a fact.

The learner definition and source definition are separate artifacts.

Source evidence remains authoritative.

IMPORTANT VALIDATOR LIMIT:

Do not pretend ordinary deterministic code can mathematically prove semantic equivalence between arbitrary free-form paraphrases.

The validator must rigorously enforce objective constraints.

It must not claim to solve semantic truth where it cannot.

Use architecture and evidence-reference constraints to minimize hallucination risk, but document this residual limitation honestly.


======================================================================
16. USAGE / REGISTER LABELS
======================================================================

Preserve source-backed labels such as:

    figurative
    slang
    informal
    formal
    dated
    archaic
    obsolete
    rare
    regional
    technical
    vulgar
    offensive
    humorous
    literary
    etc.

GPT may organize their presentation.

GPT may not invent them.

GPT may not silently remove a material qualifier.

If near-duplicate source senses have materially different usage/register qualifiers, either:

- preserve that distinction explicitly in the synthesized representation; or
- keep the learner meanings separate.

Implement an objective representation/validation rule where feasible rather than asking the validator to infer semantic appropriateness.


======================================================================
17. ARCHAIC / OBSOLETE / RARE MEANINGS
======================================================================

These meanings are first-class supported evidence.

Never discard them.

Default learner ordering may prefer contemporary meanings.

However:

- historically important archaic meanings may be promoted when useful;
- any promoted historical meaning must remain clearly labeled;
- remaining historical/rare/obsolete meanings must remain in Additional Meanings.

The Product Owner specifically values access to archaic meanings.

"Complete" means complete supported underlying meaning coverage, not dumping every raw source sense into the default screen.


======================================================================
18. SYNONYMS
======================================================================

Synonyms must be meaning-specific.

Do not generate one flat synonym list for an entire lemma.

Example:

    meaning: move quickly on foot
        sprint
        race
        jog

    meaning: operate/function
        operate
        function
        work

A synonym may be presented only if supported by supplied Wiktionary or WordNet evidence.

Each presented synonym must retain evidence provenance sufficient for deterministic validation.

GPT may associate supported synonym evidence with the learner meaning it applies to.

GPT may not invent synonyms.

If association is not supportable with sufficient confidence, do not guess.

Keep Wiktionary sense evidence and WordNet synset evidence conceptually separate; never assume a Wiktionary sense equals a WordNet synset.


======================================================================
19. WORDNET
======================================================================

WordNet remains parallel supplemental semantic evidence.

Do not flatten WordNet directly into Wiktionary senses.

For large words, WordNet may be separately chunked using deterministic structure and the same tested size discipline.

WordNet may support:

- semantic relationships;
- synonym evidence;
- concept distinctions.

It must not become a replacement lexical authority for Wiktionary evidence.


======================================================================
20. EXAMPLES
======================================================================

Dictionary examples must be source-derived.

GPT-OSS may select an existing supplied example for presentation.

GPT-OSS must not invent a dictionary example sentence.

Prefer returning example evidence IDs and using source text rather than asking GPT to regenerate the sentence.

If no source example exists:

    no example

is valid.

AI-generated practice examples may be considered in a later learning feature, but they are outside the canonical lexical synthesis path in this milestone.


======================================================================
21. ETYMOLOGY
======================================================================

Etymology should remain a separate synthesis section.

GPT may create a concise learner-friendly summary using supplied etymology evidence.

It must preserve:

- uncertainty;
- competing derivations;
- source languages;
- historical forms;
- borrowing paths;

only when supported.

It may not choose a disputed origin as fact.

If canonical evidence contains no supported etymology:

    leave it absent.

The final synthesized etymology must retain supporting evidence IDs.


======================================================================
22. FORMS / INFLECTIONS
======================================================================

Forms and inflections are factual source data.

Prefer deterministic assembly for these fields.

GPT may organize supported forms if needed but must not create them.

Examples:

    run
        runs
        running
        ran

    bright
        brighter
        brightest

If the source contains a very large technical inflection structure, retain it in the underlying output while allowing a future UI to show a useful subset.

Do not turn forms into a broad semantic navigation system.


======================================================================
23. LIMITED BASE / ROOT WORD ASSISTANCE
======================================================================

Implement only a narrow source-supported base/root relationship capability.

Examples of intended behavior:

    running -> run
    runner  -> run
    happiness -> happy

only where canonical lexical evidence directly supports the morphological/derivational relationship.

Do NOT generalize this into arbitrary semantic dependencies.

Do not produce:

    arboreal -> tree
    physician -> doctor
    scarlet -> red

merely because another word helps explain the definition.

Do not silently lemmatize exact lookup.

The exact searched word remains its own lexical entry.

GPT may use only base-word candidates that are supported by canonical source evidence.

Probe at least:

    running
    runner
    happiness

and document what the current production evidence actually supports.

If the production evidence does not safely support one of these links, report that gap rather than inventing the relationship.


======================================================================
24. PRONUNCIATION
======================================================================

The Product Owner only needs U.S. English pronunciation.

Canonical pronunciation behavior for future UI:

- expose source-backed U.S. IPA where available;
- retain other regional pronunciation evidence in canonical source data;
- do not surface UK/Australian/etc. pronunciation in the normal learner result;
- do not ask GPT to invent IPA;
- if no supported U.S. IPA exists, omit IPA;
- gTTS remains the audible pronunciation mechanism;
- do not imply gTTS audio is a direct rendering of a specific Wiktionary IPA record.

Prefer deterministic extraction/assembly for pronunciation rather than AI synthesis.

Do not modify gTTS in M004.4.


======================================================================
25. FINAL MEANING ORDER
======================================================================

Preserve canonical source order internally.

Learner presentation order is separate.

GPT may reorder final supported meanings for learner usefulness.

General preference:

    core contemporary meanings
    other contemporary meanings
    figurative / specialized meanings
    rare / regional / dated meanings
    archaic / obsolete meanings

This is a presentation policy, not a rewrite of source evidence.

Every reordered meaning must remain traceable to its original source sense IDs.


======================================================================
26. FINAL ASSEMBLY RESPONSIBILITY
======================================================================

Use the implementation approach that is safest and simplest.

Preferred architecture:

    deterministic structure/completeness/provenance
    +
    bounded GPT semantic organization
    +
    deterministic validation

Ordinary code should control wherever objective:

- POS boundaries;
- source identity;
- evidence IDs;
- source coverage;
- exact duplicates;
- schema;
- labels where mechanically known;
- forms;
- U.S. pronunciation evidence;
- dataset/model/prompt identities;
- cache identity.

GPT should be used where semantic judgment is genuinely valuable:

- learner-friendly paraphrase;
- near-duplicate consolidation;
- core vs additional sense selection;
- learner ordering;
- supported synonym association;
- concise etymology summarization.

The exact internal merge design is secondary to the controlling rule:

    No result is accepted unless it passes deterministic validation.


======================================================================
27. STRUCTURED SYNTHESIS CONTRACT
======================================================================

Define and version an explicit machine-readable synthesis schema.

Do not rely on free-form prose parsing.

The exact repository implementation may adapt to existing conventions, but the resulting validated candidate must be able to represent at least:

TOP-LEVEL SYSTEM ENVELOPE, SET BY CODE:

    schema_version
    normalized_lemma
    evidence_set_hash
    Wiktionary dataset/version identity
    WordNet identity
    GPT model identity/digest where available
    prompt/policy version
    packer version
    validator version
    inference parameters
    attempt/generation identity
    validation status

LEXICAL CONTENT:

    POS sections

For each POS:

    core meanings
    additional meanings

For each learner meaning:

    learner definition
    supporting source sense IDs
    source-backed usage labels
    supported synonym relationships
    selected source example IDs where applicable

OTHER SECTIONS:

    etymology summaries + evidence IDs
    forms/inflections + evidence provenance
    narrow base/root links + evidence provenance
    U.S. pronunciation evidence
    complete source-sense coverage/accounting

Do not force the model to generate system metadata that ordinary code already knows.

Code should attach authoritative dataset/model/prompt/hash identities itself.


======================================================================
28. DETERMINISTIC VALIDATOR
======================================================================

Implement a deterministic validator as an independent boundary.

It must validate objective requirements such as:

STRUCTURE
    - valid structured output;
    - expected schema version;
    - required fields present;
    - valid types;
    - bounded counts/sizes;
    - no unknown sections/values where schema is closed.

EVIDENCE
    - every referenced source sense ID exists;
    - every referenced evidence ID exists;
    - every output POS is supported;
    - every usage label claimed as source-backed is supported;
    - every synonym term/evidence relationship is supplied;
    - every example ID exists in supplied evidence;
    - no AI-created example masquerades as source evidence;
    - every etymology evidence reference exists;
    - every form/inflection is source-supported;
    - every base/root link is source-supported;
    - U.S. pronunciation data is source-supported.

COVERAGE
    - every supplied source sense is explicitly accounted for;
    - every learner meaning has supporting source sense IDs;
    - no silent source-sense disappearance.

BOUNDARIES
    - no cross-POS consolidation;
    - core/additional structure obeys policy/schema;
    - historical/register distinctions are preserved according to the mechanical representation rules implemented;
    - all technical provenance/version fields are code-controlled, not model-invented.

Do not use GPT-OSS itself as the deterministic validator.

Do not silently mutate a failed model result into a passing result.


======================================================================
29. VALIDATOR FAILURE SEMANTICS
======================================================================

A validator failure is a fundamental system/AI pipeline failure.

Behavior:

    synthesis
        |
        v
    deterministic validator
        |
        +-- PASS
        |      -> valid candidate
        |
        +-- FAIL
               -> system failure
               -> no cache insertion
               -> no automatic repair
               -> no hidden retry
               -> no automatic re-prompt
               -> retain diagnostic evidence
               -> return structured failure state

M004.5 will later render invalid results distinctly and disable:

    Save
    Retry
    Refresh
    Flag

Do not implement that UI now.

M004.4 must expose enough structured failure information for M004.5 to do so correctly.


======================================================================
30. FAILURE DIAGNOSTICS
======================================================================

Failures must be useful for root-cause analysis.

Record sufficient state to reproduce/investigate a failed attempt, including where available:

    timestamp
    normalized lemma
    attempt ID
    synthesis stage
    chunk ID if applicable
    evidence_set_hash
    dataset identity
    source evidence IDs involved
    model identity/digest
    prompt version
    schema version
    packer version
    validator version
    inference parameters
    seed if supported
    raw model result or durable pointer to it
    validator error codes/messages

Do not store large mutable diagnostics inside the canonical immutable lexical dataset.

Do not put runtime/generated failure logs into Git.

Use an external application runtime/data location appropriate to the existing system and document it.

Keep diagnostics bounded/manageable.

Repeated failures are valuable diagnostic signals and must not be hidden by automatic regeneration.


======================================================================
31. INFERENCE ATTEMPTS / SEEDS
======================================================================

A system validation failure is not automatically retried.

However, a later user-initiated fresh query of the same word must be capable of being a genuinely new inference attempt where the serving runtime permits that.

Therefore:

- record inference parameters;
- record seed where supported;
- permit an explicit new seed / inference attempt;
- permit a diagnostic exact-seed reproduction path if the runtime supports it.

Do not build an invisible "keep trying until something validates" mechanism.


======================================================================
32. VALIDATED SYNTHESIS CACHE
======================================================================

Implement a bounded cache for validated unsaved synthesis results.

Ordinary repeated lookup of the same unsaved word should not repeatedly consume GPU resources or produce stochastic wording changes when all relevant inputs are unchanged.

Cache identity must include at least:

    normalized lemma
    evidence_set_hash
    GPT model identity/version
    prompt/policy version
    synthesis schema version

Include other inference-policy identities if they materially affect correctness.

Rules:

SEARCH
    same cache identity
        -> reuse validated cached synthesis

RETRY SEMANTIC
    same evidence
    deliberate fresh synthesis
        -> bypass normal cache

REFRESH SEMANTIC
    different canonical evidence_set_hash
        -> distinct synthesis identity

INVALID RESULT
    -> never reusable cache

M004.5 will expose actual Retry/Refresh buttons.

For M004.4, implement/test the engine semantics or service API needed to support them without building the UI.

Do not create a complex user-visible version tree for transient retry candidates.

Do not modify PostgreSQL merely to implement this cache unless the existing architecture already provides an unquestionably appropriate location. Prefer a bounded external runtime cache for this milestone.


======================================================================
33. SAVED-WORD SEMANTICS TO PRESERVE FOR M004.5
======================================================================

Do not implement accepted-entry persistence here, but design M004.4 interfaces so the following future behavior is possible without architectural reversal:

    search unsaved word
        -> validated current synthesis

    search saved word
        -> saved accepted PostgreSQL entry automatically

    Retry on saved word
        -> same saved evidence hash
        -> new synthesis candidate

    Refresh on saved word
        -> available only when current canonical evidence hash differs
           materially from saved evidence hash
        -> new-evidence synthesis candidate

    candidate
        -> must validate
        -> Save makes candidate the new active accepted version
        -> prior accepted version remains internally recoverable

Do not silently regenerate accepted saved entries.


======================================================================
34. REFRESH HASH SUPPORT
======================================================================

There is no need to implement a dataset-update scheduler in M004.4.

The current product direction is approximately six-month lexical dataset updates with manual operator update capability.

M004.4 only needs to preserve/support the identity comparison needed later:

    saved evidence_set_hash
        vs
    current evidence_set_hash

A globally newer Wiktionary dataset does not by itself mean a word requires Refresh.

Refresh should eventually appear only when that specific word's canonical evidence has materially changed.


======================================================================
35. UI DIRECTION — ARCHITECTURAL NOTE ONLY
======================================================================

Do not redesign the learner UI in M004.4.

However, do not constrain synthesis structures around the existing Streamlit layout.

The Product Owner explicitly authorizes M004.5 to reformat the lexical UI to fit the new design.

Desktop and mobile may use different presentation formats.

They must expose the same underlying lexical capabilities and semantics, but they do not need identical layouts.

Expected future information layers include:

NORMAL VIEW
    concise core meanings

SHOW ALL MEANINGS
    every distinct validated learner-facing meaning
    grouped by POS
    labels preserved

SOURCE DETAILS
    underlying Wiktionary / WordNet evidence
    human-readable provenance

ADVANCED / DEBUG
    evidence IDs
    hashes
    model/prompt/schema versions
    technical diagnostics

Do not implement these UI layers now; ensure M004.4 output can support them.


======================================================================
36. SOURCE PROVENANCE VISIBILITY
======================================================================

Maintain enough provenance for future one-click source inspection.

Human-readable future source details should be able to identify:

    Wiktionary production dataset
    WordNet
    which evidence supports a section/meaning

Technical details such as:

    raw evidence IDs
    hashes
    synset IDs
    model versions
    prompt versions

may remain in an advanced/debug layer.

M004.4 must preserve them internally.


======================================================================
37. TEST STRATEGY
======================================================================

Add deterministic unit/characterization tests.

Do not make ordinary automated tests depend on stochastic live GPT behavior.

Use fake/stub model-provider responses to test:

- packer;
- chunking;
- schema parsing;
- validator;
- coverage accounting;
- cache identity;
- cache hit behavior;
- retry cache bypass;
- invalid-result non-caching;
- failure diagnostics;
- POS boundaries;
- label validation;
- synonym evidence validation;
- example evidence validation;
- forms;
- base/root links;
- U.S. pronunciation extraction;
- single-stage path;
- multi-stage path.

Add malformed/hostile model-output tests for at least:

    invalid JSON / structure
    unknown evidence ID
    missing source sense
    unsupported POS
    unsupported synonym
    unsupported usage label
    unsupported form
    unsupported root/base link
    invented example
    bad coverage
    cross-POS consolidation
    missing required provenance envelope
    oversized/invalid result

All such cases must hard-fail validation.

No auto-repair.


======================================================================
38. LIVE PRODUCTION PROBES
======================================================================

Provide a bounded live CLI/probe path that uses:

    active production lexical dataset
    current local GPT-OSS:20b runtime

Do not wire this into learner UI yet.

At minimum test:

SIMPLE / LOW POLYSEMY
    archipelago

INFLECTED / FORM RELATIONSHIP
    running

HIGH POLYSEMY
    run

VERY HIGH / DIFFICULT POLYSEMY
    set

BASE-WORD PROBES
    runner
    happiness

Also include at least one word with useful historical/archaic evidence if the production dataset provides an appropriate example during focused testing.

For each live test record:

    source entry count
    source sense count
    WordNet count where applicable
    evidence_set_hash
    number of generated evidence packages
    package token/size estimates
    synthesis stages used
    model inference time by stage
    total synthesis time
    validation result
    cache behavior
    output core meanings per POS
    output additional meanings per POS
    complete coverage result

Do not treat a particular GPT wording as a deterministic test assertion.

Test structure, provenance, coverage, and validation instead.


======================================================================
39. PERFORMANCE / RESOURCE OBSERVATION
======================================================================

Measure rather than guess.

Capture at least:

LEXICAL RETRIEVAL
    simple word
    complex word

PACKING
    elapsed time
    package counts

GPT INFERENCE
    per-call latency
    total latency
    number of calls

RESOURCE USE
    GPU memory where practical
    host memory where practical
    obvious swap/resource-pressure events

CACHE
    first lookup latency
    repeated cached lookup latency

Pay particular attention to "run", because production lexical lookup has already been measured around 4-5+ seconds before GPT inference.

Do not silently perform a broad lexical-engine optimization project.

If retrieval becomes an obvious blocker, make only a small safe optimization if well understood and covered by tests; otherwise document the measured bottleneck and recommend a separate focused milestone.


======================================================================
40. ATTRIBUTION / LICENSING
======================================================================

Do not remove or obscure source attribution required by the lexical architecture.

M004.4 should retain dataset/version identities needed for future learner-facing attribution.

Do not copy large source text into generated logs unnecessarily.


======================================================================
41. SECURITY / OPERATIONAL BOUNDARIES
======================================================================

- Local lexical evidence remains local.
- Local GPT inference should remain local.
- Do not introduce an external AI API.
- Do not expose the model service publicly.
- Do not expose PostgreSQL publicly.
- Do not commit runtime secrets.
- Do not commit model cache data.
- Do not commit generated synthesis caches.
- Do not commit failure logs.
- Do not commit production lexical artifacts.
- Do not change NAS behavior.
- Do not alter unrelated Docker services.
- Do not use sudo unless genuinely necessary and report why before doing so.


======================================================================
42. EXPECTED IMPLEMENTATION SHAPE
======================================================================

Fit names to the current repository, but preserve clear boundaries roughly equivalent to:

    lexical evidence retrieval
            |
            v
    deterministic evidence packer
            |
            v
    model provider adapter
            |
            v
    synthesis service
            |
            v
    deterministic validator
            |
            +----> validated cache
            |
            +----> failure diagnostics

Avoid putting all new logic back into app.py.

Maintain the bounded-refactor architecture established in M003.


======================================================================
43. ACCEPTANCE CRITERIA
======================================================================

M004.4 may be considered implementation-complete only if all of the following are true:

[ ] Mandatory reconnaissance-only checkpoint was completed.

[ ] Product Owner explicitly authorized Phase B implementation after reviewing reconnaissance.

[ ] No implementation changes were made before that authorization.

[ ] Canonical repo/branch preflight confirmed.

[ ] Existing M004.2 / M004.3 / M004.3.1 architecture preserved.

[ ] Existing local GPT-OSS:20b serving mechanism identified and documented.

[ ] Model access isolated behind a bounded provider interface.

[ ] Prompt/policy/schema/packer/validator versions explicit.

[ ] Production evidence can be converted into deterministic bounded packages.

[ ] Chunking is source-order-preserving and non-random.

[ ] Chunk limits are based on tested model input capacity, not arbitrary sense count.

[ ] Small lexical records use a bounded simple path.

[ ] Large lexical records use deterministic staged/chunked synthesis.

[ ] POS boundaries are preserved.

[ ] WordNet remains separate supplemental evidence.

[ ] Every source sense is accounted for.

[ ] Every learner meaning is source-supported.

[ ] Core and Additional Meanings are both represented.

[ ] Historical/archaic/rare meanings are not silently discarded.

[ ] Synonyms are source-supported and sense-specific.

[ ] Examples are source-derived only.

[ ] Etymology claims are source-supported.

[ ] Forms are source-supported.

[ ] Base/root links are narrow and source-supported.

[ ] U.S.-only learner pronunciation metadata is supported without AI-generated IPA.

[ ] Deterministic validator rejects objective unsupported claims.

[ ] Validator does not falsely claim semantic-proof capability for free-form paraphrases.

[ ] Any validator failure is a hard system failure.

[ ] No automatic repair/retry loop exists.

[ ] Failed results are logged sufficiently for root-cause analysis.

[ ] Invalid results never enter validated cache.

[ ] Valid repeated unsaved searches reuse cache.

[ ] Retry semantics can bypass cache using the same evidence set.

[ ] Evidence-hash identity supports future Refresh semantics.

[ ] No PostgreSQL accepted-entry/version schema change was made.

[ ] No learner UI change was made.

[ ] No translation work was performed.

[ ] Existing tests remain green.

[ ] New focused deterministic tests pass.

[ ] Live GPT probes succeed sufficiently to demonstrate the architecture, or any genuine runtime blocker is documented precisely.

[ ] Performance/resource observations are recorded.

[ ] app/PostgreSQL existing services remain healthy.

[ ] git diff contains only milestone-related files.


======================================================================
44. STOP / ESCALATION CONDITIONS
======================================================================

Stop and report rather than improvising if any of these occur:

1. Production lexical schema cannot support stable evidence provenance required by the synthesis contract.

2. Stable source-sense coverage cannot be maintained through chunking.

3. The serving runtime cannot provide a safely bounded GPT-OSS interface without material infrastructure changes.

4. Required context limits are too small for even safe staged synthesis.

5. A proposed implementation would require changing PostgreSQL accepted-entry schema before M004.5.

6. Correct implementation would require learner UI changes.

7. The base/root relationship requested cannot be derived from canonical evidence and would require model-memory inference.

8. Deterministic validation would require pretending to solve semantic equivalence that ordinary code cannot objectively verify.

9. A necessary change materially alters the M004.2 lexical-authority architecture.

10. A broad unrelated refactor becomes necessary.

11. Reconnaissance materially contradicts an assumption in this prompt such that the planned implementation shape is no longer clearly safe.

12. Implementation would require model/runtime reconfiguration that was not identified and approved at the reconnaissance checkpoint.

Do not paper over any of these.


======================================================================
45. CLOSEOUT
======================================================================

Prepare a milestone closeout following the repository's established milestone-document convention.

The closeout should include:

1. Executive result:
       PASS / PARTIAL / BLOCKED

2. Git preflight:
       branch
       HEAD
       origin/main
       working-tree state

3. Reconnaissance checkpoint:
       findings
       date/state of checkpoint
       confirmation no implementation changes preceded authorization
       Product Owner authorization to continue

4. Files changed and why.

5. Final architecture implemented.

6. Local GPT runtime:
       serving mechanism
       endpoint/interface type
       exact model identity/digest if available
       context settings
       inference settings
       seed support
       structured-output behavior

7. Token-budget determination:
       theoretical/runtime context
       reserved prompt/output/headroom
       chosen hard ceiling
       chosen target package budget
       token-count method

8. Evidence packing:
       grouping rules
       chunk rules
       oversize-sense behavior

9. Structured synthesis schema.

10. Validator rules.

11. Failure semantics and diagnostic location.

12. Cache identity and behavior.

13. Production live probe table for:
       archipelago
       running
       run
       set
       runner
       happiness
       chosen archaic-rich test word

14. For each probe, report:
       source counts
       evidence hash
       package count
       timings
       final validation status
       core/additional counts
       coverage status

15. Performance/resource observations.

16. Base/root evidence findings.

17. U.S. pronunciation handling.

18. Automated test commands and exact results.

19. Existing application/PostgreSQL health checks.

20. Known limitations, especially:
       inability of deterministic code to prove semantic equivalence
       any latency bottlenecks
       any production evidence gaps
       any model-runtime limitations

21. Confirmation that:
       no learner UI was changed;
       no translation work was done;
       no PostgreSQL acceptance/version schema was changed;
       no automatic validator repair/retry exists;
       no external AI service was introduced.

22. Exact suggested Product Owner validation commands.

23. Final git status / diff summary.

Do not commit or push.

Return the closeout and validation instructions for Product Owner review.


======================================================================
46. CONTROLLING PRODUCT SEMANTICS SUMMARY
======================================================================

Preserve these semantics for the next milestone:

SEARCH
    best currently known valid result

RETRY
    same evidence set
    same evidence_set_hash
    new synthesis attempt
    only for a valid candidate / saved entry workflow

REFRESH
    new canonical evidence
    different evidence_set_hash
    new synthesis candidate
    only when that specific word's evidence materially changed

SAVE
    accept the currently displayed valid candidate
    make it the active saved version
    retain prior accepted version internally

FLAG
    valid result but user finds it unsatisfactory
    one-click user signal
    separate from system validation failure

SYSTEM VALIDATION FAILURE
    objective pipeline failure
    automatically logged
    not user-flagged
    not cacheable
    not saveable
    no Retry/Refresh/Flag action on that failed candidate
    no automatic repair
    user must perform a new query if they want another attempt

SAVED WORD REQUERY
    show accepted saved entry automatically
    do not regenerate it silently

These user-facing controls are M004.5 work, but M004.4 must establish an engine that supports them cleanly without architectural reversal.


END M004.4 PROMPT
```
