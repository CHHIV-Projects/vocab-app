M004.6.9 is now complete and accepted as PASS. Based on the milestone sequence and the accepted M004.6.9 closeout, the next milestone is M004.6.10. I am proceeding with that next prompt below.

# M004.6.10 — Pairwise Consolidation Persistence & Resumable State Foundation

**Prompt filename:** `milestones/004.6.10_pairwise_consolidation_persistence_and_resumable_state_foundation_prompt.md`

**Required closeout filename:** `milestones/004.6.10_pairwise_consolidation_persistence_and_resumable_state_foundation_closeout.md`

**Project:** Vocab App
**Repository:** `/home/chuck/projects/vocab-app`
**Primary branch:** `main`
**Milestone type:** Additive persistence foundation
**Database migration authorized:** YES — additive only
**Production Qwen activation authorized:** NO
**Provider integration authorized:** NO
**Workflow activation authorized:** NO
**UI change authorized:** NO
**Background worker activation authorized:** NO
**Production routing change authorized:** NO
**Git Stage/Commit/Push/Tag/Merge authorized:** NO

# 1. Objective

M004.6.9 implemented and validated the provider-independent deterministic consolidation domain:

canonical evidence
    ->
deterministic partition
    ->
stable aliases
    ->
exhaustive logical pairs
    ->
validated pair judgments
    ->
exact coverage validation
    ->
deterministic conservative complete-link topology
    ->
stable learner-container identities

M004.6.10 must add the durable PostgreSQL persistence foundation needed to support that architecture safely across:

- application restart;
- provider interruption;
- partial progress;
- duplicate requests;
- future foreground/background races;
- retry;
- large partitions;
- eventual asynchronous processing.

This milestone must implement persistence for:

1. consolidation jobs;
2. pair judgments;
3. validated pair-result identity/provenance;
4. container snapshots;
5. resumable state;
6. idempotent logical identities;
7. status constraints;
8. additive migration safety;
9. persistence tests.

It must NOT yet activate live pairwise Qwen inference or route normal user searches through the new architecture.

# 2. Controlling Architecture

Treat the final accepted M004.6.8 and M004.6.9 closeouts as controlling.

The accepted identity chain is:

partition identity
    ->
logical pair identity
    ->
pair-judgment input identity
    ->
validated pair-judgment result identity
    ->
topology identity
    ->
container membership identity

Downstream stages remain separate:

topology identity
    ->
learner wording identity
    ->
ranking/final candidate identity

Important boundaries:

- the logical pair ID is structural;
- changing model/contract/evidence/options does not redefine the logical pair;
- pair-judgment identity owns semantic invocation compatibility;
- validated result identity includes the canonical decision/reason result;
- topology identity depends on the complete validated result set;
- accepted saved lexical versions remain immutable;
- partial consolidation must never masquerade as complete;
- `uncertain` is a completed semantic result but blocks a merge;
- retryable or terminal pair failure blocks normal topology finalization;
- PostgreSQL durable state must eventually be authoritative for resumability.

# 3. Required Context

Before implementation, read:

1. `docs/context/vocab_app_project_context_v2.md`
2. `docs/context/vocab_app_architecture_v2.md`
3. `docs/context/project_workflow_v2.md`
4. `coding_agent_rules_v2.md`
5. M004.5 final closeout
6. M004.6.8 final closeout
7. M004.6.9 final closeout
8. `vocab_consolidation.py`
9. `tests/test_vocab_consolidation.py`
10. `vocab_persistence.py`
11. `vocab_migrations.py`
12. existing PostgreSQL migrations
13. current persistence tests
14. this prompt

Reconnoiter the current database schema before choosing migration numbering or names.

Do not assume `003` or any other migration number unless the repository confirms it is next.

# 4. Mandatory Git Boundary

DO NOT:

- stage;
- commit;
- push;
- tag;
- merge;
- rebase;
- reset;
- create/delete branches;
- rewrite history.

Read-only Git commands are permitted.

At completion report literal:

`git status --short`

# 5. Preflight

Record:

## Repository

- repository;
- branch;
- HEAD;
- upstream;
- literal starting `git status --short`.

## Database/migrations

Confirm:

- current migration files in deterministic order;
- current applied migration state in isolated test database if available;
- current tables/constraints for:
  - `lexical_entries`;
  - `lexical_entry_versions`;
  - `lexical_flags`;
- migration runner behavior;
- PostgreSQL version used by project tests/runtime.

## Existing persistence

Identify current persistence conventions for:

- UUID/text/integer IDs;
- timestamps;
- JSON/JSONB;
- transactions;
- row factories;
- uniqueness conflicts;
- immutable saved versions;
- connection lifecycle;
- test database isolation.

## M004.6.9 domain

Confirm the exact current symbols/types for:

- partition identity;
- logical pair identity;
- pair-judgment identity;
- validated result identity;
- topology identity;
- container identity;
- decision/reason enums;
- coverage validation.

If the actual implementation differs materially from the accepted M004.6.9 closeout, STOP and report before migration work.

# 6. Scope

Implement only the persistence and resumable-state foundation.

Expected work may include:

- one additive SQL migration;
- persistence/domain adapter code;
- focused PostgreSQL persistence tests;
- small offline-domain integration helpers where required;
- milestone closeout.

Do not activate this persistence path in normal production search yet.

# 7. Explicit Non-Scope

Do NOT implement:

- live Qwen calls;
- `OllamaProvider` changes;
- GPT-OSS replacement;
- workflow routing;
- Streamlit generating state;
- background worker loop;
- queue processing;
- polling UI;
- learner wording restructuring;
- Core/Additional sequencing changes;
- cache activation in normal search;
- Retry/Refresh UI behavior;
- model fallback;
- embeddings;
- semantic screening;
- Redis job queues;
- Celery;
- corpus-wide warming.

If persistence implementation requires any of these, STOP and report.

# 8. Additive Migration Only

The migration must be additive.

Do NOT:

- drop existing tables;
- rename existing M004.5 tables;
- rewrite saved versions;
- mutate existing accepted lexical JSON snapshots;
- remove existing constraints;
- destructively transform current user data.

Existing:

- `lexical_entries`;
- `lexical_entry_versions`;
- `lexical_flags`

must continue to function unchanged.

The new structures support future consolidation work alongside the existing accepted-version boundary.

# 9. Required Logical Entities

Implement durable structures for at least:

1. `lexical_consolidation_jobs`
2. `lexical_pair_judgments`
3. `lexical_consolidation_containers`

A fourth attempt/event table is optional only if storing attempt history cleanly in pair rows would violate immutability or overwrite useful diagnostics.

Prefer the smallest adequate schema.

# 10. Consolidation Job Purpose

A consolidation job represents one complete consolidation computation identity for one lexical target/evidence state.

It should answer:

> What deterministic consolidation work are we performing or resuming?

A job is NOT:

- a user session;
- a Streamlit session;
- a raw provider request;
- an accepted saved lexical version.

It is durable semantic-processing state.

# 11. Consolidation Job Identity

Define a stable logical identity compatible with M004.6.8/M004.6.9.

At minimum include or reference:

- normalized lemma;
- evidence-set identity/hash;
- lexical dataset identity where required;
- deterministic partition manifest identity;
- consolidation contract identity;
- relevant topology contract identity.

Do not use timestamps or random runtime request IDs as the semantic uniqueness boundary.

A database surrogate primary key may exist, but it must not replace the deterministic semantic identity.

Recommend a dedicated canonical identity/hash column such as conceptually:

`consolidation_identity`

with a unique constraint.

# 12. Multi-Partition Word Handling

One searched word may contain multiple POS/material partitions.

Clarify and implement one of these structures:

A. one job per whole lexical word/evidence set with multiple partition records represented beneath it;

or

B. one durable job per deterministic partition.

Choose the design that best matches M004.6.9 and minimizes ambiguity.

The database must allow exact determination of:

- which partition a pair belongs to;
- expected pairs for that partition;
- whether all partitions for a word are complete.

Do not blur partition identity.

Document the decision in the closeout.

# 13. Recommended Job Fields

Adapt to repository conventions, but the durable job should capture enough state for future resume/idempotency.

Potential fields:

- database primary key;
- consolidation identity;
- normalized lemma;
- evidence-set hash;
- lexical dataset identity;
- partition/manifest identity;
- consolidation contract version;
- topology contract version;
- status;
- expected pair count;
- completed validated pair count;
- failed pair count;
- topology identity when complete;
- created timestamp;
- updated timestamp;
- completion timestamp where applicable;
- last error classification/summary where useful.

Do not add speculative fields without clear future use.

# 14. Job Status Model

Use explicit constrained status values.

The exact implementation may align with PostgreSQL conventions, but conceptually support states such as:

- `created`
- `running`
- `complete_validated`
- `failed_retryable`
- `failed_terminal`

If topology/container materialization is persisted as part of the same job, an additional state such as:

- `topology_validated`

may be appropriate.

Do NOT include wording/ranking states unless the table is deliberately defined as broader than consolidation.

Prefer keeping this milestone's state boundary at consolidation.

Document the exact chosen state machine.

# 15. Pair Judgment Purpose

Each pair row represents one structural pair plus one semantic judgment identity and its durable execution/result state.

It must preserve the distinction between:

- logical pair identity;
- semantic judgment input identity;
- validated judgment result identity.

Never collapse these into one opaque ID.

# 16. Pair Judgment Required Identity Fields

Persist at minimum:

- job/consolidation reference;
- partition identity;
- logical pair ID;
- source sense A ID;
- source sense B ID;
- pair-judgment identity;
- pair-judgment contract version;
- semantic model identity;
- semantic options identity;
- evidence identity;
- status.

For validated results also persist:

- decision;
- reason;
- validated result identity.

Use canonical ordered source IDs.

# 17. Pair Judgment Status

Support explicit durable status sufficient for later resume:

- `pending`
- `running`
- `succeeded_validated`
- `failed_retryable`
- `failed_terminal`

Only `succeeded_validated` satisfies semantic coverage.

A row with:

- `merge`;
- `keep_separate`;
- or `uncertain`

must still be `succeeded_validated` before it satisfies coverage.

Do not equate “row exists” with “judgment complete.”

# 18. Validated Result Constraints

For `succeeded_validated` rows require the semantic result fields needed by M004.6.9:

- decision;
- reason;
- validated result identity.

For incomplete/failed/pending rows, result fields may be null as appropriate.

Use database CHECK constraints where cleanly enforceable.

Do not attempt to encode the entire semantic compatibility matrix in SQL if it belongs in Python domain validation.

Database constraints should enforce structural integrity.

Python remains responsible for semantic contract validation.

# 19. Pair Uniqueness

Design uniqueness so the same logical semantic work cannot be duplicated accidentally.

At minimum prevent duplicate:

- job/partition/logical pair;
- judgment identity within its compatible job context.

Consider which unique constraints are needed for:

- structural pair uniqueness;
- semantic judgment uniqueness.

A future retry should update/resume the same logical semantic work rather than create arbitrary duplicate rows.

Do not prevent historical re-judgment under a genuinely new semantic identity.

# 20. Pair Attempt Provenance

Determine the smallest safe approach for future retry provenance.

Two reasonable designs:

A. Pair row stores current durable result plus bounded last-attempt metadata.

B. Separate immutable attempt table records each provider attempt.

For M004.6.10, choose based on actual requirements and repository simplicity.

The future architecture needs enough provenance to diagnose:

- transport failure;
- malformed output;
- retry count;
- model identity;
- request/schema identity;
- raw response where retained.

Do not over-engineer a general event-sourcing system.

If attempt history is deferred, state exactly what will be retained now and what belongs to M004.6.11 provider integration.

# 21. Running / Lease Fields

Future workers will need to recover abandoned `running` work after restart.

Design minimal lease-capable fields now if doing so avoids a later breaking migration.

Potential fields:

- lease owner/token;
- lease acquired time;
- lease expiry time;
- attempt count.

Do not implement the worker.

Do not depend on these fields for current production behavior.

If lease fields are deferred instead, explain why additive migration later remains safe.

# 22. Container Snapshot Purpose

A persisted container snapshot represents the deterministic topology output produced only after complete compatible pair coverage.

It must NOT contain semantic decisions invented independently from the pair ledger.

Container membership must be reproducible from:

- partition identity;
- complete validated pair-result set;
- topology policy.

# 23. Container Fields

Persist enough to identify a container deterministically:

- job/consolidation reference;
- partition identity;
- container identity;
- topology identity;
- canonical ordered member source-sense IDs;
- container schema version;
- validation/completion state if needed;
- created timestamp.

Member IDs may be stored in JSONB/array form if consistent with project conventions.

Do not persist learner wording in this table.

Do not persist Core/Additional ranking in this table.

# 24. Container Uniqueness

Require:

- unique container identity within compatible topology/job context;
- no duplicate container record for the same deterministic membership/topology;
- topology identity linkage.

Exact source-sense exclusivity across containers may be more appropriate to validate transactionally in Python than through complicated SQL constraints.

Document where database integrity ends and Python topology validation begins.

# 25. No Partial Topology Publication

Do not persist an active/final topology as though valid unless:

- expected pair coverage is complete;
- every pair is `succeeded_validated`;
- all pair identities are compatible;
- M004.6.9 coverage validator passes;
- deterministic clustering succeeds;
- exact source coverage passes.

If the persistence API allows storing provisional container rows for tests, they must be explicitly non-final and impossible to mistake for validated topology.

Prefer storing final containers only after successful complete topology assembly.

# 26. Transaction Boundary

Define a safe transaction for final consolidation completion.

Conceptually:

1. lock/read compatible job;
2. verify exact pair coverage from durable rows;
3. reconstruct M004.6.9 domain judgments;
4. run coverage validator;
5. compute complete-link topology;
6. validate exact source coverage;
7. persist container snapshots;
8. persist topology identity;
9. mark job complete/topology_validated;
10. commit atomically.

If any validation fails:

- rollback;
- job must not become complete.

Implement the persistence transaction if within this milestone scope.

No provider is involved.

# 27. Persistence API

Add a narrow persistence API following existing repository style.

Likely operations include equivalents of:

- create-or-get consolidation job;
- load job by deterministic identity;
- initialize expected pair rows;
- load pair judgments;
- claim/update pair state for future use;
- store validated judgment result;
- record failure state;
- calculate/load progress;
- finalize topology from persisted results;
- load validated containers.

Names should fit repository conventions.

Avoid a generic repository framework.

# 28. Idempotent Job Creation

Creating the same compatible consolidation job more than once must converge on the same logical durable job.

Test:

first request
    ->
creates job

second identical request
    ->
returns same logical job

It must not create duplicate semantic work.

Use database uniqueness plus conflict-safe transaction behavior.

# 29. Idempotent Pair Initialization

Initializing expected pairs repeatedly must not create duplicates.

Given the same job/partition and M004.6.9 pair set:

first initialization
    ->
creates expected rows

second initialization
    ->
no duplicate logical rows
    ->
same durable identity set

If an incompatible pair set is supplied for an existing immutable job identity:

STOP/reject rather than silently mutate semantic membership.

# 30. Pair State Transition Safety

Define allowed persistence transitions.

At minimum prevent nonsensical regressions such as:

`succeeded_validated -> pending`

unless a new semantic judgment identity/job is explicitly created.

Reasonable future paths include:

pending
    -> running
    -> succeeded_validated

pending
    -> running
    -> failed_retryable
    -> running

pending/running/failed_retryable
    -> failed_terminal

The persistence layer should enforce legal transitions in Python even if not every transition is encoded in SQL.

Test invalid transition rejection.

# 31. Immutable Successful Judgment

Once a specific pair-judgment identity has a `succeeded_validated` result, do not silently overwrite:

- decision;
- reason;
- validated result identity;
- semantic identity.

If the same semantic input is somehow judged differently later, this is a provenance/conflict condition requiring an explicit new result/version policy.

For the initial implementation:

> successful validated judgment is immutable.

Test it.

# 32. Resume Behavior

Implement/read enough durable state so a later worker can ask:

- what jobs are incomplete?
- which expected pairs are still pending/retryable?
- which pair results are already valid and reusable?
- is topology already complete?
- what failed terminally?

M004.6.10 does not need to execute resumed work.

It only needs to make resume state durable and queryable.

# 33. Restart Independence

Tests should demonstrate that after closing/reopening a database connection:

- created job remains;
- pair state remains;
- successful validated decisions remain;
- incomplete work is discoverable;
- completed topology remains loadable.

Do not rely on process memory.

# 34. Existing M004.5 Boundary

Do not connect the new consolidation records directly to the active saved lexical version unless there is an obvious nullable provenance link that is safe and additive.

M004.6.11 or later will integrate consolidation provenance into the existing save/version workflow.

For now preserve:

existing saved lexical version behavior
    unchanged

new consolidation persistence
    isolated foundation

If a future foreign key is likely, design IDs cleanly so one can be added later without data rewrite.

# 35. Saved Entry Immutability

Verify with regression tests or schema inspection that the new migration does not change:

- active saved versions;
- existing version JSON;
- existing save behavior;
- existing lexical entry uniqueness;
- existing flags.

No existing saved record should be rewritten simply because the migration is applied.

# 36. Migration Roll-Forward Safety

Use the project's normal migration mechanism.

The migration should be safe on:

- a fresh database;
- an existing database already at M004.5 schema.

Run isolated migration tests for both paths if the test infrastructure supports them.

Do not manually mutate the developer database outside the normal migration runner except for isolated disposable test setup.

# 37. Migration Idempotency

Follow current migration-runner semantics.

Do not rely on rerunning raw DDL blindly if migrations are tracked as one-time applied units.

Test or inspect that:

- migration is recorded/applied once;
- repeated `apply_migrations` does not duplicate structures;
- existing schema remains valid.

# 38. Foreign-Key Behavior

Choose explicit delete/update behavior.

Do not allow deletion of a job to leave orphaned pair/container records.

Prefer conservative relational integrity.

Because semantic provenance may matter, consider whether completed jobs should normally be retained rather than cascading away casually.

Document chosen FK behavior.

# 39. JSON / Raw Provider Fields

Because no provider integration occurs yet, do not fabricate raw provider responses.

If schema includes nullable future fields such as:

- raw response JSON;
- request payload hash;
- provider diagnostics;

leave them null in offline tests unless specifically testing persistence shape.

Do not let speculative diagnostics dominate the schema.

# 40. Semantic Enums

Reuse the M004.6.9 decisions/reasons exactly:

Decision:

- `merge`
- `keep_separate`
- `uncertain`

Reason:

- `paraphrase_or_duplicate`
- `contextual_variant`
- `specialized_subsense`
- `broader_or_narrower`
- `related_but_distinct`
- `materially_different`
- `insufficient_evidence`

Do not rename them.

Prefer Python/domain validation as semantic authority.

SQL may use CHECK constraints for allowed values if doing so remains maintainable.

# 41. Identity Storage

Store deterministic identities as explicit columns rather than relying only on opaque JSON snapshots.

At minimum identities used for uniqueness/resume should be directly queryable/indexable:

- consolidation identity;
- partition identity;
- logical pair ID;
- pair-judgment identity;
- validated result identity;
- topology identity;
- container identity.

Use suitable indexes.

Do not prematurely index every metadata field.

# 42. Progress Accounting

A job's progress must be derivable from pair rows.

If denormalized counts are stored on the job:

- expected pair count;
- completed validated count;
- failed count;

they must not become an independent source of truth that can silently diverge.

Either:

A. compute counts from pair rows when needed;

or

B. maintain denormalized counts transactionally and test consistency.

Recommend the simpler safe initial approach.

# 43. One-Sense Persistence

A one-sense partition has:

- zero pair rows;
- one deterministic container;
- valid topology without semantic provider work.

The persistence model must support this cleanly.

Do not require at least one pair row for a complete job.

Test one-sense finalization.

# 44. Multiple Containers

Test persistence/finalization for a partition producing:

- multiple independent learner containers;
- one multi-sense merged container plus singleton(s).

Load-back must preserve exact deterministic membership and identities.

# 45. Non-Transitive Fixture Persistence

Reuse the important M004.6.9 fixture:

A-B = merge
B-C = merge
A-C = keep_separate

Persist all three validated judgments.

Finalize topology.

Expected:

- deterministic safe topology;
- never `{A,B,C}` as one container;
- persisted containers exactly match M004.6.9 engine output.

Also test `A-C = uncertain`.

# 46. Incomplete Coverage Finalization Rejection

Persist a multi-sense job with one missing or non-successful pair.

Attempt topology finalization.

It must fail and persist no final topology.

Test at least:

- missing pair;
- `pending`;
- `failed_retryable`;
- `failed_terminal`;
- incompatible judgment identity.

# 47. Successful Judgment Conflict

Test:

1. persist `succeeded_validated` pair:
   
   - decision = merge;
   - reason = contextual_variant.

2. attempt to overwrite same pair-judgment identity with:
   
   - decision = keep_separate.

Expected:

- reject conflict;
- original successful judgment remains unchanged.

Do not silently “last write wins.”

# 48. Transaction Rollback Test

Create a finalization scenario that fails validation after transaction start.

Verify:

- no partial container set remains;
- job is not marked complete;
- existing valid pair rows remain intact.

# 49. Test Database Isolation

Follow the established project practice for PostgreSQL tests.

Do not run destructive persistence tests against normal development/user data.

Use:

- isolated schema/database;
- transaction rollback;
- disposable container/database;

whichever is already established by the project.

If the repository lacks a safe PostgreSQL testing pattern, STOP and report rather than improvising against live data.

# 50. Required Tests

Add focused tests for at least:

1. migration applies to fresh schema;
2. migration applies after existing M004.5 migrations;
3. repeat migration runner is safe;
4. existing M004.5 tables remain;
5. job creation;
6. idempotent create-or-get;
7. duplicate consolidation identity rejected/converged;
8. pair initialization;
9. pair initialization idempotency;
10. pair uniqueness;
11. valid pair state transitions;
12. invalid transition rejection;
13. successful validated judgment persistence;
14. successful judgment immutability;
15. `uncertain` persistence;
16. restart/reload behavior;
17. progress derivation;
18. incomplete coverage rejection;
19. incompatible identity rejection;
20. one-sense zero-pair finalization;
21. multi-container finalization;
22. non-transitive topology finalization;
23. persisted topology identity;
24. persisted container identity;
25. transaction rollback;
26. existing save/version regression.

Reuse M004.6.9 offline domain fixtures/helpers where practical.

# 51. Regression Validation

Run:

- new consolidation persistence tests;
- `tests.test_vocab_consolidation`;
- relevant persistence/migration tests;
- relevant M004.5 save/version tests;
- smallest useful workflow regression proving current behavior remains unchanged.

Do not alter current production expectations to accommodate inactive new tables.

Run:

`git diff --check`

Report exact commands and literal results.

# 52. No Production Activation

Verify that normal application execution still does NOT:

- create consolidation jobs;
- create pair rows;
- call Qwen;
- load new containers;
- route search through new topology;
- change Save;
- change Retry;
- change Refresh;
- show generating state.

The new persistence path remains dormant unless called explicitly by tests/future implementation code.

# 53. File Scope

Expected changed files should remain approximately:

- one new additive migration;
- `vocab_persistence.py` or a narrowly dedicated consolidation persistence module;
- focused tests;
- milestone closeout;
- possibly a small migration-test helper if already consistent with repository patterns.

Do not make broad changes to:

- `app.py`;
- `vocab_workflow.py`;
- `vocab_synthesis.py`;

unless an unavoidable import/type issue is discovered.

If broad modifications become necessary, STOP and report.

# 54. Recommended Persistence Module Boundary

Prefer separating consolidation persistence from existing accepted-entry save logic if this avoids turning `vocab_persistence.py` into a large mixed-responsibility module.

A reasonable additive module might be:

`vocab_consolidation_persistence.py`

However, follow repository conventions.

The key requirement is conceptual separation:

accepted learner-version persistence
    !=
in-progress consolidation-work persistence

# 55. No Background Worker Yet

Do not implement:

- infinite loops;
- polling;
- daemon processes;
- thread workers;
- process pools;
- Redis queues;
- Celery;
- scheduled warmers.

M004.6.10 provides the durable state such a worker will eventually consume.

# 56. No Provider Adapter Yet

Do not modify:

- `OllamaProvider`;
- model defaults;
- `think` behavior;
- provider schema;
- runtime model loading.

M004.6.11 will address the bounded Qwen provider contract after persistence is accepted.

# 57. No Workflow Routing Yet

Do not wire `LexicalWorkflow.search()` to:

- create jobs;
- check job state;
- return generating state.

That comes after provider orchestration and persistence are proven independently.

# 58. Stop / Escalation Conditions

STOP and report if:

- existing migrations do not provide a safe additive path;
- persistence tests cannot be isolated from user/development data;
- current PostgreSQL conventions conflict materially with the proposed entity model;
- one job per partition versus one job per word cannot be resolved safely from current architecture;
- the M004.6.9 domain identities cannot be stored/reconstructed faithfully;
- idempotent uniqueness cannot be expressed without changing accepted saved-version semantics;
- successful judgment immutability conflicts with current persistence conventions;
- topology finalization cannot be transactional;
- implementing persistence requires live provider integration;
- implementing persistence requires workflow/UI changes;
- migration would destructively touch existing M004.5 data.

Do not solve these silently.

# 59. Required Closeout

Create:

`milestones/004.6.10_pairwise_consolidation_persistence_and_resumable_state_foundation_closeout.md`

Include:

## 1. Status

PASS / PARTIAL / FAIL.

## 2. Starting State

Repository/branch/HEAD/status/database migration state.

## 3. Persistence Reconnaissance

Existing schema, conventions, test isolation.

## 4. Migration

Exact migration file and additive structures.

## 5. Job Model

Identity, fields, status, uniqueness.

## 6. Pair Judgment Model

Structural ID versus semantic judgment versus validated result identities.

## 7. Container Model

Topology/container identity and membership storage.

## 8. Job/Partition Granularity Decision

One job per partition or broader manifest model, with rationale.

## 9. State Transitions

Allowed and rejected transitions.

## 10. Idempotency

Job and pair create-or-get behavior.

## 11. Successful Judgment Immutability

Conflict behavior.

## 12. Resume Semantics

What is durably discoverable after restart.

## 13. Coverage / Finalization Transaction

Exact transaction boundary and validation.

## 14. One-Sense Behavior

Zero-pair persistence/finalization.

## 15. Complete-Link Persistence

Multi-container and non-transitive fixtures.

## 16. Existing M004.5 Compatibility

Proof saved/versioned behavior remains unchanged.

## 17. Tests

Commands and literal results.

## 18. Regression Validation

Existing tests plus `git diff --check`.

## 19. No-Activation Confirmation

No provider/workflow/UI/worker activation.

## 20. Files Changed

Literal list.

## 21. Final Git Status

Literal `git status --short`.

## 22. Next Milestone Recommendation

Confirm readiness for Qwen provider/orchestration integration.

## 23. STOP Gate

Product Owner / Architect review.

# 60. PASS Criteria

M004.6.10 may close PASS when:

1. migration is additive;
2. existing M004.5 schema/data remain valid;
3. deterministic consolidation jobs are durably represented;
4. structural logical pair identity is preserved;
5. pair-judgment input identity is stored separately;
6. validated result identity is stored separately;
7. pair rows are unique/idempotent;
8. explicit pair statuses are enforced;
9. successful validated judgments are immutable;
10. resumable incomplete work is queryable;
11. one-sense zero-pair topology is supported;
12. incomplete coverage blocks finalization;
13. compatible complete coverage finalizes transactionally;
14. topology/container identities persist correctly;
15. non-transitive complete-link behavior matches M004.6.9;
16. restart/reload tests succeed;
17. finalization rollback is safe;
18. normal production behavior remains unchanged;
19. no live Qwen/provider/workflow/UI activation occurs;
20. tests and regressions pass.

# 61. Expected Next Milestone

If M004.6.10 passes, expected next milestone:

**M004.6.11 — Bounded Qwen Pair-Judgment Provider & Orchestration Integration**

Likely scope:

- explicit Qwen model/options adapter;
- `think: false`;
- closed structured pair schema;
- one-pair request construction;
- response validation;
- retry/error classification;
- persistence of attempts/results;
- resumable orchestration over pending pair rows;
- bounded live smoke tests;

while still keeping broad user-facing workflow activation separate if useful.

Do not implement M004.6.11 now.

# 62. Final Instruction

M004.6.9 established correct deterministic semantics.

M004.6.10 establishes durable state.

The milestone boundary is:

deterministic partition
    ->
durable consolidation identity
    ->
durable expected pair rows
    ->
durable validated fixture judgments
    ->
exact persisted coverage
    ->
transactional deterministic topology finalization
    ->
durable container snapshots
    ->
restart-safe reload

STOP THERE.

No live Qwen.
No provider modification.
No normal workflow activation.
No UI change.
No background worker.
No learner wording integration.
No Core/Additional integration.
No embeddings.
No screening.
No Git Stage/Commit/Push/Tag/Merge.

Create the required closeout and STOP for Product Owner / Architect review.

**End of M004.6.10 prompt**
