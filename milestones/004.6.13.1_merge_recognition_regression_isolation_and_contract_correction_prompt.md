# M004.6.13.1 — Merge Recognition Regression Isolation & Contract Correction

**Prompt filename:** `milestones/004.6.13.1_merge_recognition_regression_isolation_and_contract_correction_prompt.md`

**Required closeout filename:** `milestones/004.6.13.1_merge_recognition_regression_isolation_and_contract_correction_closeout.md`

**Project:** Vocab App
**Repository:** `/home/chuck/projects/vocab-app`
**Primary branch:** `main`

**Milestone type:** Formal corrective semantic-provider milestone
**Parent milestone:** M004.6.13
**Parent status:** PARTIAL pending this corrective milestone
**Blocks:** learner wording/Core-Additional integration
**Live bounded Qwen diagnostic calls authorized:** YES
**Prompt/schema correction authorized:** YES, but only after controlled isolation
**Model change authorized:** NO
**Topology/persistence redesign authorized:** NO
**Database migration authorized:** NO
**Learner workflow/UI activation authorized:** NO
**Git Stage/Commit/Push/Tag/Merge authorized:** NO

# 1. Immediate Instruction

STOP M004.6.13 as a completed/PASS milestone.

Treat M004.6.13 as:

**PARTIAL — contract-invalid outputs were successfully eliminated, but positive merge behavior under the hardened contract was not validated.**

Do NOT discard the M004.6.13 implementation.

Preserve all current M004.6.13 work exactly as it exists and perform this corrective milestone on top of it.

M004.6.13.1 must determine why two already-adjudicated MERGE fixtures changed to `keep_separate` under the new v2 configuration and make the smallest safe correction if the cause is isolated.

Do NOT proceed to learner wording or Core/Additional ranking until M004.6.13.1 closes PASS.

# 2. Why This Corrective Milestone Exists

M004.6.13 successfully fixed a real reliability defect:

v1:
    model could emit:
        merge + specialized_subsense
    ->
    application rejects
    ->
    identical deterministic retry
    ->
    repeated rejection
    ->
    failed_terminal
    ->
    word blocked

v2:
    structured schema + application allowlist
    ->
    only contract-compatible decision/reason pairs are accepted

This worked.

Real previously failing `gecko` and `heron` cases returned contract-valid results on the first attempt.

However, targeted follow-up validation exposed a new concern.

Two already-adjudicated MERGE reference fixtures:

1. `charge` noun
   
   - accepted reference:
     `merge / paraphrase_or_duplicate`
   - independently Architect-adjudicated MERGE

2. `cleave` verb
   
   - accepted reference:
     `merge / paraphrase_or_duplicate`

both returned:

`keep_separate`

under the current v2 prompt/schema.

This means the contract-hardening machinery is safe structurally, but we do not yet know whether:

A. the new v2 prompt wording suppresses legitimate merges;
B. the new `oneOf` schema suppresses legitimate merges;
C. the combination of prompt + schema creates the effect;
D. the fixed production seed/configuration already behaved this way under v1;
E. the reference/model disagreement is independent of M004.6.13.

M004.6.13.1 exists to isolate those possibilities rather than guessing.

# 3. Central Question

M004.6.13.1 must answer:

> Did the M004.6.13 prompt/schema hardening cause a regression in legitimate merge recognition, and if so, what is the smallest correction that preserves the full seven-combination compatibility contract without reintroducing invalid decision/reason outputs?

The desired final behavior is:

known paraphrase/duplicate
    ->
valid merge

known materially distinct/specialized senses
    ->
valid keep_separate

insufficient evidence
    ->
valid uncertain

and NEVER:

merge + specialized_subsense
merge + broader_or_narrower
merge + related_but_distinct
merge + materially_different
or any other incompatible decision/reason combination.

# 4. Controlling Semantic Policy

Do NOT change the product's semantic policy.

The accepted policy remains:

"Merge only near-paraphrase, duplicate, or contextual-application variants. Keep specialized, broader/narrower, related-but-distinct, materially different, and uncertain senses separate. Prefer false splits."

The seven valid semantic combinations remain:

## MERGE

- `merge / paraphrase_or_duplicate`
- `merge / contextual_variant`

## KEEP SEPARATE

- `keep_separate / specialized_subsense`
- `keep_separate / broader_or_narrower`
- `keep_separate / related_but_distinct`
- `keep_separate / materially_different`

## UNCERTAIN

- `uncertain / insufficient_evidence`

Do NOT weaken this allowlist.

The correction must preserve it.

# 5. Required Context

Before changing anything, read:

1. `docs/context/vocab_app_project_context_v2.md`
2. `docs/context/vocab_app_architecture_v2.md`
3. `docs/context/project_workflow_v2.md`
4. `coding_agent_rules_v2.md`
5. M004.6.4 final closeout
6. M004.6.9 final closeout
7. M004.6.11 final closeout
8. M004.6.12 final closeout
9. M004.6.13 prompt
10. latest M004.6.13 closeout
11. `vocab_consolidation.py`
12. `vocab_consolidation_provider.py`
13. relevant M004.6.4 benchmark manifests/reference artifacts
14. `reference_review_adjudicated_architect.csv`
15. relevant current provider/domain tests
16. this prompt

Do not rerun model selection, embeddings research, or consolidation architecture research.

# 6. Mandatory Git Boundary

DO NOT:

- Stage;
- `git add`;
- Commit;
- Push;
- Tag;
- Merge;
- Rebase;
- Reset;
- Restore;
- Stash;
- create/delete branches;
- rewrite history.

Product Owner controls Git publication.

Read-only Git commands are permitted.

# 7. Preflight

Record:

- repository;
- branch;
- HEAD;
- upstream;
- literal `git status --short`.

The current worktree is expected to contain the uncommitted M004.6.13 implementation.

Identify exactly which changes pre-date M004.6.13.1.

Do NOT treat those files as unrelated dirty-worktree contamination.

Preserve them.

Also confirm:

- Ollama version;
- `qwen3:8b` installed digest;
- current semantic options;
- current v1 prompt;
- current v2 prompt;
- current v1 flat schema;
- current v2 `oneOf` schema;
- current v1 and v2 contract constants;
- current full seven-combination application validator.

Expected model digest remains:

`500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`

If digest differs:

STOP.

# 8. Preserve Current M004.6.13 Safety Improvements

Do NOT revert the following merely to restore merge behavior:

- full seven-combination application allowlist;
- contract-version-aware v1/v2 validation;
- v1 historical backward compatibility;
- v1/v2 identity separation;
- application-side authoritative validation.

These are accepted improvements.

The question is specifically whether the MODEL-facing prompt/schema needs correction.

# 9. Phase A — Exact Historical/Current Contract Extraction

Before any live call, record the exact text/schema for:

## Configuration A — Historical v1

- exact v1 prompt/policy wording;
- v1 flat decision/reason schema;
- seed 5100;
- think=false;
- temperature=0;
- num_predict=256;
- num_ctx=16384.

## Configuration D — Current v2

- exact current v2 prompt/policy wording;
- current `oneOf` compatibility schema;
- same semantic options.

Do NOT paraphrase these for the experiment.

Use exact repository/accepted historical content.

# 10. Phase B — Controlled Factorial Isolation

Use the SAME real pair evidence, SAME model, SAME digest, SAME seed, SAME temperature, SAME context, and SAME output budget.

Vary ONLY:

1. prompt wording;
2. schema shape.

Test these four configurations:

## A — Historical baseline

v1 prompt
+
v1 flat schema

## B — Schema-only hardening

v1 prompt
+
v2 `oneOf` schema

## C — Prompt-only hardening

v2 prompt
+
v1 flat schema

## D — Current v2

v2 prompt
+
v2 `oneOf` schema

Configuration D already has live results for the two primary fixtures from M004.6.13.

If the exact evidence/model/options are identical, reuse those recorded D results rather than spending duplicate live calls.

# 11. Primary Diagnostic Fixtures

Use two already-adjudicated MERGE fixtures.

Do NOT invent new labels.

## Fixture 1 — `charge` noun

Use the exact M004.6.3/M004.6.4 sense pair already documented in M004.6.13:

- accepted decision: `merge`
- accepted reason: `paraphrase_or_duplicate`
- independently Architect-adjudicated MERGE

## Fixture 2 — `cleave` verb

Use the exact pair already documented in M004.6.13:

- accepted decision: `merge`
- accepted reason: `paraphrase_or_duplicate`

Preserve the exact source evidence used in the prior validation.

# 12. Initial Live Diagnostic Call Budget

For Fixtures 1 and 2:

A, B, C require at most:

2 fixtures × 3 new configurations = 6 new calls

D should normally reuse the existing M004.6.13 results.

Before making calls, report:

- fixture;
- configuration;
- exact prompt identity;
- exact schema identity;
- expected calls.

Do not make duplicate calls merely for symmetry.

# 13. Experimental Results Table

Record results as:

| Fixture | Prompt | Schema | Decision        | Reason          | Contract-valid? |
| ------- | ------ | ------ | --------------- | --------------- | --------------- |
| charge  | v1     | v1     | ...             | ...             | ...             |
| charge  | v1     | v2     | ...             | ...             | ...             |
| charge  | v2     | v1     | ...             | ...             | ...             |
| charge  | v2     | v2     | existing result | existing result | yes             |
| cleave  | v1     | v1     | ...             | ...             | ...             |
| cleave  | v1     | v2     | ...             | ...             | ...             |
| cleave  | v2     | v1     | ...             | ...             | ...             |
| cleave  | v2     | v2     | existing result | existing result | yes             |

Do not persist these hybrid diagnostic configurations into the production consolidation ledger.

They are experiment configurations, not canonical semantic contracts.

Use a controlled direct diagnostic path.

# 14. Cause Interpretation

Use the evidence, not intuition.

## Prompt-side regression indicated if:

A merges
B merges
C separates
D separates

Interpretation:

v2 prompt wording chilled merge recognition.

## Schema-side regression indicated if:

A merges
C merges
B separates
D separates

Interpretation:

`oneOf` structured schema altered model behavior.

## Prompt/schema interaction indicated if:

A merges
B merges
C merges
D separates

Interpretation:

neither change alone causes the regression, but their combination does.

## No v2-specific regression indicated if:

A also separates on the accepted MERGE fixtures under current production seed/options.

In that case, do NOT blame M004.6.13 automatically.

Proceed to the bounded seed diagnostic in Section 15.

# 15. Conditional Seed Diagnostic

ONLY if historical A:

v1 prompt + v1 schema + seed 5100

also fails to merge both primary MERGE fixtures:

test historical configuration A using:

- seed 5101;
- seed 5102;

for the same two fixtures.

Maximum:

2 fixtures × 2 additional seeds = 4 calls.

Do NOT alter any other semantic option.

Purpose:

determine whether the apparent merge-recognition issue predates v2 and is instead sensitive to the production single-seed choice that was selected after earlier three-seed benchmark work.

If seed 5101/5102 materially changes merge recognition:

STOP after characterization.

Do NOT automatically change the production seed based on two fixtures.

Report that broader seed-policy validation is required.

If all historical seeds also fail the accepted MERGE fixtures:

STOP and report reference/model divergence.

Do not force the model to match the references by prompt manipulation.

# 16. Optional Third MERGE Fixture

If the A/B/C/D result is ambiguous rather than clearly attributable, one additional already-Architect-adjudicated MERGE fixture is authorized.

Preferred candidate:

the externally adjudicated `cleave` crystal-cleavage pair (indices 2/4) already identified in M004.6.13.

Do NOT invent a new fixture.

Apply only the smallest set of configurations needed to resolve the ambiguity.

# 17. Total Diagnostic Call Budget

Maximum live Qwen calls for M004.6.13.1:

**16**

This is a NEW corrective-milestone budget.

Do not count calls previously spent in M004.6.13 against this new diagnostic budget.

However:

- do not spend calls unnecessarily;
- reuse exact recorded results when legitimate;
- stop once cause is clear.

No corpus-wide benchmark.

# 18. Phase C — Correction Rules

Do NOT implement a correction until Phase B identifies the cause.

Then follow the appropriate branch.

# 19. Correction Branch A — Prompt Is the Cause

If evidence shows:

v1 prompt + v2 schema
    ->
restores known MERGE behavior

while:

v2 prompt
    ->
suppresses it,

then implement the smallest correction:

- preserve the full seven-combination v2 schema;
- preserve the full application allowlist;
- restore/reuse the exact accepted historical semantic-policy wording that did NOT chill merge recognition;
- remove only the additional v2 wording demonstrated to cause the regression.

Do NOT weaken the schema.

Because prompt semantics changed relative to v2, create:

`PAIR_JUDGMENT_CONTRACT_V3 = "pair-judgment-v3-exhaustive-qwen"`

and make v3 the new default.

v1 and v2 remain historical and loadable.

# 20. Correction Branch B — `oneOf` Schema Is the Cause

If the v2 `oneOf` schema itself demonstrably suppresses legitimate merges:

do NOT return to the unconstrained v1 cross-product schema as production behavior.

Instead evaluate one narrowly authorized structurally safe alternative:

## Seven-state judgment enum

Conceptually:

{
  "judgment": "<one of exactly seven canonical judgment values>"
}

For example, seven enum values corresponding exactly to:

- merge / paraphrase_or_duplicate
- merge / contextual_variant
- keep_separate / specialized_subsense
- keep_separate / broader_or_narrower
- keep_separate / related_but_distinct
- keep_separate / materially_different
- uncertain / insufficient_evidence

The exact serialized token names are implementation details, but they must be:

- deterministic;
- closed;
- reversible by code;
- unambiguous.

Code then maps the single canonical enum value deterministically into the existing:

`decision`
+
`reason`

domain representation.

The application validator still runs after mapping.

This alternative must NOT alter persistence/domain result shape.

If implementing this alternative requires broad domain/persistence changes:

STOP.

# 21. Correction Branch C — Prompt/Schema Interaction

If each change works independently but the combination suppresses merges:

prefer the simplest configuration already demonstrated to satisfy BOTH:

1. contract safety;
2. merge recognition.

Strong preference:

historical accepted prompt
+
structurally constrained schema

if that B configuration succeeds.

Do not invent additional prompt prose if removing the unnecessary prose solves the interaction.

# 22. Correction Branch D — Historical Baseline Also Fails

If historical v1 prompt + historical schema under the current production options also fails to recognize known MERGE fixtures:

do NOT alter v2 merely to force these two references.

Run only the conditional seed diagnostic in Section 15.

Then STOP and report.

This is no longer proven to be an M004.6.13 regression and may require a separately scoped semantic/seed validation decision.

Do not claim M004.6.13.1 PASS unless a safe production contract is demonstrated.

# 23. Contract Versioning

Any final model-facing prompt/schema change after v2 must create v3.

Do NOT overwrite semantic meaning under the existing v2 identity.

Required historical behavior:

v1
    ->
historical narrow validator semantics

v2
    ->
full allowlist + original M004.6.13 prompt/schema behavior

v3
    ->
corrected production contract from M004.6.13.1

All remain independently identifiable.

Existing v1 and v2 persisted data remain untouched.

# 24. Validator Versioning

The application-side full seven-combination allowlist remains authoritative for v2 and v3.

Do NOT regress v3 to the v1 four-item blocklist.

Expected:

v1:
    historical blocklist compatibility

v2:
    full seven-combination allowlist

v3:
    full seven-combination allowlist

If the current implementation uses a generic "v2+" behavior safely, it may remain.

Test v1/v2/v3 explicitly.

# 25. Identity Requirements

Explicitly prove:

same pair + same evidence + v1
    !=
same pair + same evidence + v2
    !=
same pair + same evidence + v3

Also prove:

same v3 semantic inputs
    ==
same v3 identity

across repeated construction.

No old job may be silently reused under v3.

# 26. Persistence Requirements

No migration is expected.

Existing persistence identity already includes contract version.

Verify rather than assume:

- v1 remains loadable;
- v2 remains loadable;
- v3 gets distinct job/judgment identity;
- old v1/v2 rows remain immutable;
- completed old topology is not reused as v3 topology.

If a migration appears necessary:

STOP.

# 27. Phase D — Final Candidate Live Validation

After a correction is selected and implemented, validate the EXACT final candidate production contract.

Use at minimum:

## MERGE fixtures

- `charge` noun;
- `cleave` verb;

or another already-adjudicated MERGE fixture if one of those was shown during Phase B to be an unstable/reference-divergent case.

Required:

at least one accepted MERGE fixture must return:

`merge`

with:

- `paraphrase_or_duplicate`; or
- `contextual_variant`.

## KEEP-SEPARATE fixtures

Use at least two already-grounded separate-worthy real pairs from prior milestones, preferably among:

- `gecko`;
- `heron`;
- `lynx`;

where prior evidence clearly supports separation.

Required:

- contract-valid `keep_separate`;
- no unsafe merge.

# 28. Final Live Safety Gate

The final production candidate may close PASS only if targeted live evidence demonstrates BOTH:

A. legitimate merge recognition remains possible;

AND

B. previously problematic distinct/specialized pairs remain safely separate.

Do not accept a correction that merely changes the bias in the opposite direction.

# 29. Contract-Invalid Output Gate

Under the final candidate, no live tested pair may produce or persist:

- merge + specialized_subsense;
- merge + broader_or_narrower;
- merge + related_but_distinct;
- merge + materially_different;
- keep_separate + paraphrase_or_duplicate;
- keep_separate + contextual_variant;
- keep_separate + insufficient_evidence;
- uncertain + anything except insufficient_evidence.

The structured-output layer should prevent these where possible.

The application validator must still reject them if they somehow occur.

# 30. Retry Policy

Do not redesign retry semantics in this milestone unless an actual remaining contract-invalid output demonstrates it is necessary.

The primary goal is to make invalid combinations structurally unavailable.

No stochastic retry.

No seed switching during normal retry.

No prompt mutation during retry.

# 31. Production Semantic Options

Unless Phase B demonstrates seed 5100 itself is the unresolved cause, preserve:

- model: `qwen3:8b`
- digest:
  `500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`
- think: false
- temperature: 0
- seed: 5100
- num_predict: 256
- num_ctx: 16384

Do not alter semantic options merely to improve one fixture.

# 32. Offline Test Requirements

At minimum test:

1. v1 historical compatibility behavior;
2. v2 full allowlist behavior;
3. v3 full allowlist behavior;
4. full 21 decision/reason matrix remains correct under production default;
5. final provider schema accepts every valid canonical state;
6. final provider schema excludes every invalid canonical state;
7. final response deterministically maps to canonical decision/reason;
8. application validator remains authoritative;
9. malformed JSON;
10. extra fields;
11. timeout/provider errors;
12. model mismatch;
13. v1/v2/v3 identity separation;
14. v3 identity stability.

# 33. PostgreSQL / Cache Tests

Using disposable PostgreSQL 17 migration-004 schema, prove:

1. v1 completed topology remains loadable;
2. v2 completed topology remains loadable;
3. same evidence requested under v3 creates a distinct job;
4. v3 does not reuse v1/v2 topology as current work;
5. v3 completion is cacheable;
6. repeated v3 request makes zero provider calls;
7. old v1/v2 rows remain unchanged.

No production/user database.

# 34. Regression Suite

Run at minimum:

- `tests.test_vocab_consolidation`
- provider tests
- orchestrator/adapter tests
- word-level consolidation workflow tests
- persistence-shape tests
- normal vocab workflow tests
- synthesis tests
- M004.6.11 PostgreSQL tests
- M004.6.12 PostgreSQL tests

The known M004.5 candidate-identity test defect remains unrelated debt.

Do not fix it here.

# 35. No Downstream Work

Do NOT implement:

- learner wording;
- Core/Additional ranking;
- final learner candidate construction;
- Streamlit UI;
- Save/Retry/Refresh integration changes;
- background worker;
- deferred processing;
- embeddings;
- screening;
- another model.

M004.6.13.1 must finish the semantic pair contract first.

# 36. Existing M004.6.13 Closeout

If the M004.6.13 closeout exists in the repository/worktree, ensure its status accurately reflects:

**PARTIAL — contract hardening implemented, positive merge behavior unresolved; continued in M004.6.13.1.**

Do not rewrite its technical history.

M004.6.13.1 receives its own closeout.

# 37. Static Validation

Run `python3 -m py_compile` on every new/changed Python file.

Run:

`git diff --check`

Report literal results.

# 38. STOP / Escalation Conditions

STOP and report if:

- historical v1 baseline at identical production settings also fails known MERGE fixtures and seed diagnosis does not clearly resolve the cause;
- no tested safe schema representation can preserve both valid merge and valid keep-separate behavior;
- correcting merge recognition requires weakening the seven-combination allowlist;
- correction introduces an unsafe merge on known separate-worthy evidence;
- v1/v2 persisted rows become unreadable;
- v3 incorrectly reuses old v1/v2 topology;
- database migration appears necessary;
- model change appears necessary;
- more than 16 M004.6.13.1 live calls would be needed;
- downstream learner code would need to change.

Do not improvise a broader AI research program.

# 39. Required Closeout

Create:

`milestones/004.6.13.1_merge_recognition_regression_isolation_and_contract_correction_closeout.md`

Include:

## 1. Status

PASS / PARTIAL / FAIL.

## 2. Starting State

HEAD, branch, worktree, model/runtime identity.

## 3. Parent M004.6.13 State

Explain why M004.6.13 remained PARTIAL.

## 4. Diagnostic Question

Prompt vs schema vs interaction vs seed/reference divergence.

## 5. Exact v1 Configuration

Prompt/schema/options.

## 6. Exact v2 Configuration

Prompt/schema/options.

## 7. Primary MERGE Fixtures

Evidence/reference provenance.

## 8. A/B/C/D Diagnostic Matrix

Literal results.

## 9. Root-Cause Determination

Evidence-based conclusion.

## 10. Conditional Seed Diagnostic

Only if used.

## 11. Selected Correction

Exact smallest change.

## 12. v3 Contract

Prompt/schema/output representation.

## 13. Compatibility Matrix

Seven valid states.

## 14. Historical Compatibility

v1/v2 preserved.

## 15. Identity Separation

v1 != v2 != v3 proof.

## 16. Persistence/Cache Behavior

No cross-version reuse.

## 17. Offline Tests

Commands/results.

## 18. PostgreSQL Tests

Commands/results.

## 19. Final MERGE Live Validation

Known adjudicated fixtures and results.

## 20. Final KEEP-SEPARATE Live Validation

Known separate fixtures and results.

## 21. Semantic Safety Review

False-merge / false-split considerations.

## 22. Contract-Invalid Output Review

Any invalid combinations observed.

## 23. Live Call Budget

Planned versus actual.

## 24. Regression Suite

Commands/results.

## 25. Production Isolation

Unchanged topology/workflow/UI/etc.

## 26. Known Debt

M004.5 test defect and any unresolved semantic issues.

## 27. Static Validation

`py_compile` and `git diff --check`.

## 28. Files Changed

Distinguish pre-existing M004.6.13 modifications from M004.6.13.1-specific changes where possible.

## 29. Final Git Status

Literal `git status --short`.

## 30. Next Milestone Recommendation

Readiness for learner wording/Core-Additional integration.

## 31. STOP Gate

Product Owner / Architect review.

# 40. PASS Criteria

M004.6.13.1 may close PASS only when:

1. current M004.6.13 work is preserved;
2. exact v1 and v2 prompt/schema configurations are documented;
3. known adjudicated MERGE fixtures are used;
4. prompt and schema effects are isolated rather than guessed;
5. the root cause is evidence-based;
6. the full seven-combination allowlist is preserved;
7. application validation remains authoritative;
8. no invalid decision/reason combination is accepted;
9. any semantic correction receives a new v3 identity;
10. v1 historical behavior remains loadable;
11. v2 historical behavior remains loadable;
12. v3 identity is distinct and deterministic;
13. no v1/v2 persisted work is overwritten;
14. v3 does not incorrectly reuse old topology;
15. at least one known adjudicated MERGE fixture produces a valid live `merge`;
16. at least two known separate-worthy fixtures remain valid live `keep_separate`;
17. no obviously unsafe merge is introduced;
18. live diagnostic/correction calls remain within the authorized budget;
19. relevant offline tests pass;
20. relevant PostgreSQL tests pass;
21. normal word-level workflow remains unchanged;
22. no DB migration is introduced;
23. no learner wording/ranking/UI work is introduced;
24. static validation passes.

# 41. Expected Next Milestone

If M004.6.13.1 closes PASS, the consolidation semantic layer is considered ready for downstream learner integration.

Proceed next to:

**M004.6.14 — Learner Wording & Core/Additional Ranking Re-integration**

Expected flow:

validated learner containers
    ->
bounded learner-friendly wording
    ->
separate Core relevance ranking
    ->
code-computed Additional Meanings
    ->
deterministic validation

Do NOT implement M004.6.14 now.

# 42. Final Instruction

M004.6.13 fixed contract invalidity but exposed a possible over-separation regression.

M004.6.13.1 must isolate and correct that issue without sacrificing safety.

The goal is not "more merges."

The goal is:

correct merges
+
correct separations
+
no invalid decision/reason combinations
+
stable deterministic identities
+
safe historical compatibility.

Preserve the seven-state semantic contract.
Preserve v1/v2 history.
Create v3 only if a model-facing correction is required.
Do not weaken validation.
Do not change models.
Do not change persistence schema.
Do not proceed to learner wording.
Do not Stage/Commit/Push/Tag/Merge.

Create the required closeout and STOP for Product Owner / Architect review.

**End of M004.6.13.1 prompt**
