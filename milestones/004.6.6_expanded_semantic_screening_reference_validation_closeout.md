# M004.6.6 — Expanded Semantic Screening Reference Validation Closeout

## 1. Status

**PASS — expanded semantic screening validation complete; the locked `nomic-embed-text` + `definition-focused-v1` + `0.90` threshold architecture failed the required high-recall generalization gate. No production screening architecture is authorized.**

Phase A was completed with a blinded review package. Product Owner / Architect adjudication was then supplied as an external CSV and consumed as immutable input. Phase B was completed without LLM calls or production changes. The result does not authorize production integration.

## 2. Scope and boundary

This pass implemented only benchmark-local reference construction. It did not modify production synthesis, grouping, ranking, validators, persistence, lexical SQLite, WordNet, Compose, Ollama configuration, or runtime behavior.

No new embedding model, enriched representation, vector database, ANN infrastructure, Qwen/GPT-OSS inference, or Git history operation was used.

The locked M004.6.5 screening configuration was reused:

| Field | Value |
|---|---|
| Embedding model | `nomic-embed-text:latest` |
| Digest | `0a109f422b47e3a30ba2b10eca18548e944e8a23073ee3f3e947efcf3c45e59f` |
| Dimension | 768 |
| Representation | `definition-focused-v1` |
| Representation form | `{lemma} [{pos}]: {definitions joined by '; '}` |
| Normalization | Unicode NFC and deterministic whitespace collapse |
| Similarity | Cosine |
| Technical threshold reference | `0.90`, not a semantic label |

## 3. Phase A run

Final isolated artifact run:

`/home/chuck/.local/share/vocab-app/benchmarks/m004.6.6/20260910T223848Z/`

The reference constructor selected 21 words across noun, verb, and adjective partitions, including:

- high-polysemy: `run`, `set`, `bank`, `charge`;
- derived/inflected: `running`, `runner`;
- semantic-boundary: `cleave`;
- technical/specialized: `quark`;
- everyday polysemy: `light`, `head`, `field`, `line`, `point`, `strike`, `spring`, `table`, `fall`, `break`, `match`, `scale`, `seal`, `bark`;
- grammatical controls: `right`, `fast` where lexical partition availability permitted.

The selected inventory preserves complete source-sense identity and POS/material partition provenance. The known large partitions `bank:noun`, `runner:noun`, `set:verb`, and `run:verb` are included in the complete accounting.

## 4. Candidate reference distribution

| Measure | Result |
|---|---:|
| Selected words | 21 |
| Selected source partitions | 21 |
| Complete source senses | 623 |
| Complete eligible pair universe | 12,223 |
| Blinded review pairs | 200 |
| Bounded plausible-merge surfaces before adjudication | 32 |
| Semantic labels assigned | 0 |

The 200 review pairs were sampled deterministically across the requested threshold bands:

| Similarity band | Review pairs |
|---|---:|
| `>= 0.94` | 63 |
| `0.90–0.94` | 47 |
| `0.86–0.90` | 45 |
| `0.80–0.86` | 36 |
| `< 0.80` | 9 |

The candidate set therefore includes below-threshold and near-threshold material. Similarity was used only to surface review candidates and was not converted into a semantic reference decision.

The bounded plausible-merge surface count is a pre-adjudication sampling designation, not a claim that those pairs should merge. Product Owner / Architect review must determine `MERGE`, `KEEP SEPARATE`, or `UNCERTAIN`.

## 5. Blinded review artifact

Primary review artifact:

`/home/chuck/.local/share/vocab-app/benchmarks/m004.6.6/20260910T223848Z/reference_review_blinded.csv`

It contains:

- pair ID and deterministic review order;
- word and POS;
- material labels;
- source-sense IDs;
- both definitions;
- source labels;
- examples where available;
- blank `decision`;
- blank `review_notes`.

It does not expose cosine similarity, threshold-retained status, Qwen output, GPT-OSS output, or model identity.

No `reference_adjudicated_v3.json` was created.

## 6. Supporting artifacts

| Artifact | Purpose |
|---|---|
| `reference_candidates_v1.json` | Versioned machine-readable candidate reference, complete source partitions, pair provenance, and blank semantic status |
| `reference_review_blinded.csv` | Product Owner / Architect semantic review package |
| `screening_scores_technical.json` | Separate technical similarities, bands, hashes, and threshold-0.90 workload status |
| `selected_partition_inventory.json` | Word/POS/material inventory and complete-versus-sampled pair counts |
| `complete_partition_accounting.json` | Complete partition counts, similarity distributions, and threshold-0.90 workload projections |
| `source_sense_representations.json` | Exact locked representation text and SHA-256 hashes |
| `phase_a_summary.json` | Structural summary and Phase A status |

The complete-partition accounting is explicitly workload-only. Threshold-retained counts and reductions do not establish semantic correctness.

Selected complete-partition examples:

| Partition | Senses | Complete pairs | Pairs at/above 0.90 | Workload reduction |
|---|---:|---:|---:|---:|
| `bank` noun | 26 | 325 | 10 | 96.9% |
| `runner` noun | 37 | 666 | 123 | 81.5% |
| `set` verb | 45 | 990 | 49 | 95.1% |
| `run` verb | 60 | 1,770 | 259 | 85.4% |

## 7. Historical Phase B gate

During Phase A, Phase B was intentionally gated pending Product Owner / Architect adjudication of the blinded CSV. That adjudication was subsequently received as an external immutable input and Phase B was completed in Sections 10–17.

The gate required accepted labels before calculating:

- resolved merge recall;
- lost approved merge IDs;
- keep-separate reduction;
- threshold sensitivity and held-out analysis;
- positive safety margins;
- high-similarity negative and low-similarity positive analysis;
- top-K comparison;
- small-partition cost comparison;
- architecture recommendation.

No unreviewed candidate was treated as semantic ground truth.

## 8. Implementation and tests

Benchmark-only implementation:

- [m0046_expanded_reference_phase_a.py](/home/chuck/projects/vocab-app/tools/benchmarks/m0046_expanded_reference_phase_a.py)
- [m0046_expanded_reference_phase_b.py](/home/chuck/projects/vocab-app/tools/benchmarks/m0046_expanded_reference_phase_b.py)

Focused tests:

- [test_m00466_expanded_reference_phase_a.py](/home/chuck/projects/vocab-app/tests/test_m00466_expanded_reference_phase_a.py)
- [test_m00466_expanded_reference_phase_b.py](/home/chuck/projects/vocab-app/tests/test_m00466_expanded_reference_phase_b.py)
- [test_m00465_semantic_pair_screening.py](/home/chuck/projects/vocab-app/tests/test_m00465_semantic_pair_screening.py)

The tests cover:

- threshold-band boundaries;
- deterministic capped pair sampling;
- blinded artifact field separation;
- absence of semantic labels in the Phase A workflow.

## 9. Final boundary

No production changes were made. No Qwen/GPT-OSS calls were made. No hybrid policy was adopted. This closeout stops for Product Owner / Architect review because the fixed threshold did not satisfy the preferred high-recall gate.

## 10. Phase B adjudicated reference

The supplied CSV was validated before use:

| Check | Result |
|---|---|
| Pair rows | 200 |
| Exact Phase A pair-ID match | yes |
| Duplicate pair IDs | none |
| `MERGE` | 30 |
| `KEEP SEPARATE` | 164 |
| `UNCERTAIN` | 6 |
| Invalid decisions | none |
| Review notes | present for all rows |

An immutable derivative was created; the supplied CSV and prior artifacts were not modified:

`/home/chuck/.local/share/vocab-app/benchmarks/m004.6.6/20260910T234324Z/reference_adjudicated_v3.json`

## 11. Locked Phase B result

Phase B reused the exact model identity and representation configuration. Phase A did not persist raw vectors, so the exact source representations were embedded again with the unchanged model; no model, representation, or runtime configuration changed.

The primary fixed threshold `0.90` produced:

| Metric | Result |
|---|---:|
| Resolved merge pairs | 30 |
| Retained approved merges | 27 |
| **Resolved merge recall** | **90.0%** |
| Lost approved merge pairs | 3 |
| Resolved keep-separate pairs | 164 |
| Screened keep-separate pairs | 87 |
| Keep-separate reduction | 53.0% |

Lost approved merges:

- `head:noun:686e59004b8f:2ff9a624b4dbb675` — similarity `0.8925`;
- `head:noun:686e59004b8f:7c8b43290a22c0f2` — similarity `0.8993`;
- `scale:noun:69ba4527710f:9c36bcb7084472ad` — similarity `0.8273`.

The six uncertain pairs were excluded from hard metrics and reported separately.

## 12. Threshold sensitivity

| Threshold | Merge recall | Keep-separate reduction | Lost merge count |
|---:|---:|---:|---:|
| 0.86 | 96.7% | 26.8% | 1 |
| 0.87 | 96.7% | 31.7% | 1 |
| 0.88 | 96.7% | 32.9% | 1 |
| 0.89 | 96.7% | 38.4% | 1 |
| **0.90** | **90.0%** | **53.0%** | **3** |
| 0.91 | 86.7% | 55.5% | 4 |
| 0.92 | 86.7% | 62.2% | 4 |

The lowest approved merge similarity was `0.8273`, which is `0.0727` below the locked threshold. The positive safety margin is therefore not robust across this expanded reference.

## 13. Top-K and hybrid gate

| Policy | Merge recall | Keep-separate reduction |
|---|---:|---:|
| Top-K 2 | 96.7% | 26.8% |
| Top-K 3 | 100.0% | 16.5% |
| Top-K 5 | 100.0% | 6.1% |

Because threshold `0.90` lost approved merges and Top-K 3 recovered them, the explicitly permitted diagnostic hybrid was measured:

- `threshold_0.90 UNION top_k_3`;
- merge recall: `100.0%`;
- keep-separate reduction: `15.9%`;
- 64 additional retained pairs over threshold-only.

This hybrid was measured only as a diagnostic and was not adopted.

## 14. Difficult cases

The highest-similarity approved keep-separate cases reached approximately `0.9902`, including closely related senses in `line`, `match`, and `head`. This confirms that similarity is candidate-screening evidence, not semantic authority.

The low-similarity approved merges included:

- `scale` at `0.8273`;
- two `head` pairs at `0.8925` and `0.8993`;
- additional approved merges near the threshold.

These are exact review cases for future representation or screening-policy analysis. No enriched representation was introduced to rescue them.

## 15. Large-partition workload projections

These remain unlabeled workload measurements, not semantic correctness claims:

| Partition | Senses | Complete pairs | Retained at 0.90 | Reduction |
|---|---:|---:|---:|---:|
| `bank` noun | 26 | 325 | 10 | 96.9% |
| `runner` noun | 37 | 666 | 123 | 81.5% |
| `set` verb | 45 | 990 | 49 | 95.1% |
| `run` verb | 60 | 1,770 | 259 | 85.4% |

## 16. Small-partition cost projection

Using the observed complete-partition retained-pair rate, estimated Qwen pair-call counts are:

| Senses | Exhaustive pairs | Screened estimate |
|---:|---:|---:|
| 2 | 1 | 0.181 |
| 3 | 3 | 0.542 |
| 5 | 10 | 1.807 |
| 8 | 28 | 5.060 |
| 10 | 45 | 8.133 |

These are workload estimates only. Embedding overhead is separate, so exhaustive review may remain operationally simpler for small partitions.

## 17. Phase B artifacts and validation

Final Phase B artifact run:

`/home/chuck/.local/share/vocab-app/benchmarks/m004.6.6/20260910T234324Z/`

Important artifacts:

- `reference_adjudicated_v3.json`;
- `phase_b_summary.json`;
- `reference_comparison.json`.

Focused validation:

```text
12 tests passed
git diff --check passed
```

Phase B implementation and tests:

- [m0046_expanded_reference_phase_b.py](/home/chuck/projects/vocab-app/tools/benchmarks/m0046_expanded_reference_phase_b.py)
- [test_m00466_expanded_reference_phase_b.py](/home/chuck/projects/vocab-app/tests/test_m00466_expanded_reference_phase_b.py)

The expanded reference demonstrates that the locked global threshold `0.90` does not generalize to 100% approved-merge recall. Further policy or representation work requires a separately authorized milestone; this milestone makes no production recommendation.
