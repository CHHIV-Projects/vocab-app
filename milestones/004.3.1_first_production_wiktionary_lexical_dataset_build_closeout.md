# Vocab App M004.3.1 Closeout

## 1. Git/repository preflight

- Repository: `/home/chuck/projects/vocab-app`
- Branch: `main`
- HEAD: `dfcfce845f87bf967860b17317d28a245958ad4a`
- Upstream: `origin/main`
- Ahead/behind: `0 0`
- M004.3 was committed and upstream-aligned before this build.
- Product Owner authorization was provided for the first production-scale build subject to resource gates.
- No Stage, Commit, or Push was performed.

### Terminal exit reconciliation

The reported `/bin/bash` exit code `1` came from the early pre-download verification command:

```bash
sha1sum -c enwiktionary-20260901-sha1sums.txt --ignore-missing
```

At that moment the checksum sidecar had not yet been downloaded, so the command returned `1` because the checksum file was missing. This occurred before the resumed download completed and before extraction, normalization, SQLite construction, validation, or activation. The later checksum verification passed with `enwiktionary-20260901-pages-articles.xml.bz2: OK`.

## 2. Product Owner authorization

Authorization was granted to download one dated official English Wiktionary dump, use the local host filesystem, run the bounded four-worker extraction, and create/activate the first production lexical dataset if all gates passed.

The resource gate passed before download:

- selected filesystem: `/home/chuck/.local/share/vocab-lexical` on `/dev/nvme0n1p2`;
- free disk before build: approximately `1.6 TB`;
- system RAM: approximately `61 GiB`;
- available RAM before build: approximately `52 GiB`;
- CPU: 24 logical threads / AMD Ryzen 9 7900X;
- swap: 8 GiB available, negligible before build;
- Vocab application and PostgreSQL healthy;
- no unrelated cleanup or privileged filesystem changes performed.

## 3. Selected lexical-data root

```text
/home/chuck/.local/share/vocab-lexical
```

The root is outside Git, user-owned, and separate from the Vocab application image and PostgreSQL volume.

Layout used:

```text
sources/wiktionary-20260901/
work/wiktionary-20260901-retry1/
versions/wiktionary-20260901/
active
```

## 4. Source Wiktionary dump identity

- Dump: `enwiktionary-20260901-pages-articles.xml.bz2`
- Source URL: `https://dumps.wikimedia.org/enwiktionary/20260901/enwiktionary-20260901-pages-articles.xml.bz2`
- Downloaded size: `1,632,298,458` bytes
- Published SHA1: `1ed7d1f8ff20ac07349dabe2a921d411385a1e04`
- Local SHA1 verification: `OK`
- Local SHA256: `06acca8138eacb3e8ae9c1d6232f836e37c3bf9b6d582a86693731fe0d336c20`
- Download duration: not captured as one aggregate value because the transfer completed through resumed bounded curl sessions; bytes and checksums are preserved.

## 5. Wiktextract environment and configuration

External isolated environment:

```text
/home/chuck/.local/share/vocab-lexical/work/wiktextract-env
```

Pinned source identities:

- Python: `3.12.3`
- Wiktextract commit: `ccec6f120efedd84f57fe0f1631e89408e9cb62a`
- Wikitextprocessor commit: `4deed5191c9e4cb61ee1a4c822e3f6686ae8541b`
- Scribunto submodule: `d35ca1f8d5fd23f1a9915e497cc00cac238f28c4`
- Wiktextract package: `1.99.7`
- Wikitextprocessor package: `0.4.96`
- NLTK: `3.10.3`

The Scribunto submodule had to be initialized after the first failed extraction. The missing submodule caused the original `ustring:ustring` failures. The pinned commits were not changed.

Extraction configuration:

```text
--dump-file-language-code en
--language-code en
--language-code mul
--pronunciations
--linkages
--examples
--etymologies
--inflections
--num-processes 4
```

Broad translation capture and all-language capture were not enabled.

## 6. Extraction and failure/retry evidence

The first extraction was preserved and not treated as valid. It produced repeated `module 'ustring:ustring' not found` failures, approximately 4.36 MB raw JSONL, a 384 MB error report, and approximately 14.6 GB stdout. No normalization or activation used that output.

After initializing the pinned Scribunto submodule, the targeted probe succeeded:

```text
require("mw").ustring.lower("ÄBC") -> "äbc"
lua_errors 0
lua_warnings 0
```

The retry ran in a new directory:

```text
/home/chuck/.local/share/vocab-lexical/work/wiktionary-20260901-retry1
```

Retry result:

- raw Wiktextract JSONL: `2,153,482,083` bytes;
- JSONL records: `1,637,433`;
- malformed JSONL records: `0`;
- error report: `29,558,674` bytes;
- extraction stdout: approximately `18.6 MB`;
- extraction stderr: approximately `1.5 KB`;
- exit status: `0`;
- elapsed extraction time: `7:03:11.75`;
- maximum RSS: `8,780,272 KiB`;
- swap events reported by `/usr/bin/time`: `0`.

The retry had no `ustring:ustring`, missing-module, or LuaError markers. The remaining 1,640 errors and 2,208 warnings are bounded source/template/parser issues in the Wiktionary corpus, not systemic Lua-environment failure.

## 7. Normalized canonical evidence

The completed retry was streamed through the M004.3 deterministic normalizer.

- normalized record count: `1,586,623`;
- canonical `evidence.jsonl`: `5,686,274,112` bytes;
- evidence SHA256: `30dfdae235ca2ed10c1557827efdb63b98c297b890e8eab42854ff5e43431cae`;
- normalization duration: not captured as a standalone value in the retained command logs;
- input source records remained recoverable separately;
- no GPT-OSS or other AI was used.

The normalizer preserved English and relevant Translingual records, source metadata, senses, forms, sounds, etymology, relations, source locators, and source-record hashes.

## 8. SQLite production index

Production SQLite projection:

- path: `/home/chuck/.local/share/vocab-lexical/versions/wiktionary-20260901/lexical.sqlite`;
- size: `4,678,975,584` bytes;
- SHA256: `bd9c018811f392ce8b58463d6a53d4530420f9cb4777ad8ad5936816d366e4e1`;
- SQLite integrity: `ok`;
- SQLite index-build duration: not captured as a standalone value in the retained command logs;
- entries: `1,586,617`;
- senses: `1,894,561`.

Six identical duplicate source records were detected during projection. They had identical source-record hashes and were skipped in SQLite while remaining in canonical evidence. True conflicting identity/hash collisions are rejected by the projection code.

## 9. Representative production lookups

Validated against the production index:

- `archipelago`: 2 entries, 6 senses, noun/verb evidence, WordNet parallel evidence;
- `run`: 5 entries, 119 senses, multiple POS, 57 WordNet synsets;
- `running`: 6 entries, 20 senses, multiple grammatical roles, 52 WordNet synsets;
- `bright`: 5 entries, 44 senses, 11 WordNet synsets;
- `computer`: 2 entries, 6 senses, 2 WordNet synsets;
- `free`: 6 entries, 41 senses, 22 WordNet synsets.

The production lookup boundary does not silently lemmatize `running` to `run`; both exact entries remain available.

## 10. WordNet parallel-evidence validation

Authoritative pinned-environment validation used the same content-based WordNet identity model as M004.3:

```text
nltk-3.10.3-wordnet-5defd570025b7e7d
```

WordNet remained visibly separate from Wiktionary evidence. No cross-source sense equivalence was asserted.

## 11. Evidence-bundle/hash validation

Representative production lookups generated deterministic evidence bundles and hashes:

- `archipelago`: `8efbd04c6e194d0d...`
- `run`: `ecf7591bea40b99a...`
- `running`: `d103903acb3135b1...`
- `bright`: `1a51ca6fd82327bb...`
- `computer`: `5a1f1b17129f3553...`
- `free`: `62997fe76e3f0f4c...`

The canonical serializer excludes runtime timestamps, SQLite IDs, filesystem paths, and incidental iteration order.

## 12. Production performance measurements

Production exact-lemma lookup samples were measured in the pinned environment over ten read-only repetitions for `run`:

- sample count: `10`;
- minimum: `4186.92 ms`;
- median: `4241.98 ms`;
- p95: `4903.70 ms` using the inclusive percentile calculation;
- maximum: `5414.04 ms`.

The process peak RSS during this validation was `225,824 KiB`, approximately `220.5 MiB`.

These lookup times are honest production measurements of the current query implementation. They are materially slower than the fixture measurements because production entries contain much larger sense/source payloads. Lookup optimization remains a future hardening concern and did not block this first dataset build because all required lookups completed deterministically.

Final lexical-root footprint was approximately `28 GB`, including source, work logs, retry artifacts, canonical evidence, index, and external extraction environments. Final host free space remained approximately `1.5 TB`. Peak build-disk consumption was not captured as a separate high-water measurement; only starting free space, retained footprint, and final free space were captured.

## 13. Dataset manifest

Manifest: `/home/chuck/.local/share/vocab-lexical/versions/wiktionary-20260901/manifest.json`.

It records dump identity/checksums, pinned extractor identities, Scribunto identity, capture configuration, normalized record count/hash, SQLite size/hash/integrity, extraction duration/RSS/swap, WordNet identity, attribution reference, and `production_ui_integration: false`.

## 14. Licensing/attribution

Production attribution metadata is retained at:

`/home/chuck/.local/share/vocab-lexical/versions/wiktionary-20260901/attribution/LICENSES.md`

It identifies Wiktionary/Wikimedia source licensing considerations, Wiktextract MIT licensing, snapshot/source context, and the future requirement to expose attribution through the learner-facing UI.

## 15. Activation result

Before activation, no active lexical pointer existed. After every validation gate passed, the pointer was atomically written:

```text
/home/chuck/.local/share/vocab-lexical/active
```

Contents:

```text
wiktionary-20260901
```

The active database resolves to:

`/home/chuck/.local/share/vocab-lexical/versions/wiktionary-20260901/lexical.sqlite`

This was the first production activation; there was no previous production lexical version to retain or roll back to. The failed first extraction remains preserved separately from the active version.

If rollback is required for this first activation, the operator should atomically remove or rename the active pointer file `/home/chuck/.local/share/vocab-lexical/active`, leaving the validated version directory and source artifacts preserved. This returns the lexical subsystem to the prior no-active-production-dataset state. A later version rollback should replace the pointer with the prior verified dataset version.

## 16. Application/runtime regression

- Existing repository tests: `34 tests`, one expected host-side psycopg skip;
- application compile validation: passed;
- SQLite integrity: passed;
- Vocab application container: healthy;
- PostgreSQL container: healthy;
- Streamlit health endpoint: `ok`;
- PostgreSQL volume: unchanged;
- normal Dictionary UI: intentionally unchanged and not wired to production lexical data;
- gTTS and translation paths: unchanged;
- no PostgreSQL schema changes.

## 17. Temporary/retained artifacts

Retained:

- verified source dump and checksums;
- failed first-run stdout/stderr/error/raw artifacts;
- successful retry raw output and error report;
- canonical evidence JSONL;
- production SQLite;
- manifest and attribution metadata;
- pinned extractor source/environment.

Removed after the collision diagnosis was understood:

- temporary failed SQLite diagnostic files inside the production version directory.

No unrelated files, Docker images, volumes, or services were cleaned.

## 18. Remaining limitations

- The current application UI does not consume the production lexical index; that remains M004.5 work.
- Production exact lookup currently takes approximately 3.5–4.5 seconds for representative words because the result includes large multi-sense payloads. Future query projection/caching can harden this path.
- Download duration was not captured as one aggregate value because the 1.63 GB source completed through resumed bounded transfer sessions.
- The extraction error report contains bounded source/template/parser errors; no systemic Lua/module-resolution failure remained.

## 19. M004.4 readiness

The deterministic production lexical evidence foundation is ready for Product Owner/architect review before M004.4. GPT-OSS synthesis remains unimplemented and must not begin automatically.

## 20. Recommendation

M004.3.1 is PASS. The production dataset was built, validated, and atomically activated under the approved resource and safety gates. No learner-facing UI integration, PostgreSQL schema change, translation work, gTTS change, GPT-OSS synthesis, Stage, Commit, or Push was performed.

## 21. Final Git state

The production lexical artifacts are outside Git. The repository implementation remains uncommitted and unstaged for Product Owner review. No Stage, Commit, or Push was performed.
