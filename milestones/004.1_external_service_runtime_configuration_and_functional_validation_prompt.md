# Vocab App Milestone M004.1

# External Service Runtime Configuration and Functional Validation

**Prompt file:** `milestones/004.1_external_service_runtime_configuration_and_functional_validation_prompt.md`
**Required closeout:** `milestones/004.1_external_service_runtime_configuration_and_functional_validation_closeout.md`
**Mode:** Bounded corrective implementation
**Reasoning:** Medium
**Target repository:** `/home/chuck/projects/vocab-app`
**Target runtime:** containerized Vocab service on `henderson-server1`

---

# 1. Objective

Restore and validate the existing external word-enrichment functionality in the containerized Vocab service created by M004.

The Product Owner has confirmed that the always-on application currently reaches the Dictionary UI but displays:

`Missing API Key! Check secrets.`

followed by:

`Word not found.`

M004 correctly replaced Google Sheets persistence with PostgreSQL.

M004.1 must NOT restore Google Sheets.

Instead, M004.1 must determine how the current application obtains:

- definitions/dictionary results;
- translation results;
- pronunciation/audio/TTS;
- any other current word-enrichment service used by the existing UI;

and safely provide the runtime configuration required for those existing services inside the containerized environment.

The goal is:

> The self-hosted Vocab application should retain its existing external dictionary/translation/pronunciation functionality while PostgreSQL remains the sole operational persistence layer.

## Product Owner controlling clarification

This clarification supersedes conflicting provider wording in this prompt while preserving the original prompt as milestone history.

- Merriam-Webster is no longer an accepted definition provider. Do not restore `merriam_key`, repair the existing Merriam-Webster API path, or retain it as a fallback.
- The current `get_mw_data()` / Merriam-Webster implementation must be removed cleanly and replaced with the approved Free Dictionary API boundary.
- Google Sheets remains permanently removed from persistence. Do not restore `gspread`, Google service-account persistence credentials, Sheet fallback, dual-write, or synchronization.
- Retain the existing `deep_translator.GoogleTranslator` translation service, `gTTS` pronunciation/audio service, and NLTK WordNet synonym behavior unless bounded reconnaissance proves one of those implementations obsolete.
- Before selecting a definition replacement, inspect canonical source and bounded reachable historical Vocab references for an evidenced Google-backed definition provider.
- The reconnaissance found Merriam-Webster as the definition provider in canonical `app.py` and reachable archived Vocab variants. It found `GoogleTranslator`, gTTS, and WordNet as separate retained services. It found no evidenced Google-backed definition provider.
- The approved replacement definition provider is local NLTK WordNet using the already provisioned corpus. It requires no API key or network request. Do not substitute another provider.
- The existing `Missing API Key! Check secrets.` message must be traced to the obsolete `merriam_key` lookup. It must not be "fixed" by restoring that secret.

The controlling M004.1 service model is:

```text
PostgreSQL
  = canonical self-hosted persistence

GoogleTranslator
  = retained translation service

gTTS
  = retained pronunciation/audio service

NLTK WordNet
  = retained synonym source

NLTK WordNet
  = approved local definition provider; no API key or network request

Dictionary definitions
  = local WordNet primary-sense enrichment mapped into the existing application result shape
```

---

# 2. Product Boundary

There were historically two unrelated external concerns.

## Removed

Google Sheets as application persistence.

This is intentionally gone.

Normal runtime persistence remains:

    Vocab
      -> PostgreSQL

Do not reintroduce:

- Google Sheets;
- gspread persistence;
- Google service-account credentials;
- Google/PostgreSQL fallback;
- dual-write;
- synchronization.

## Retained for now

External services that provide current application functionality such as:

- definitions;
- dictionary lookup;
- translation;
- pronunciation;
- text-to-speech.

These may remain external services.

M004.1 should make the existing implementation work reliably from the container.

A later milestone may investigate local-AI/local-service replacements.

Do not perform that investigation in M004.1.

---

# 3. Starting Gate

Before changing code:

    cd /home/chuck/projects/vocab-app
    
    pwd
    git branch --show-current
    git rev-parse HEAD
    git status --short
    git rev-parse @{upstream} 2>/dev/null || true
    git rev-list --left-right --count HEAD...@{upstream} 2>/dev/null || true
    git log -5 --oneline --decorate

Confirm:

- M004 is committed;
- branch is `main`;
- working tree is clean except for the committed M004.1 prompt after Product Owner prompt lifecycle;
- local/upstream relationship is known.

If local commits have not yet been pushed, report that before implementation.

Do not reset, restore, clean, or rewrite history.

---

# 4. Repo Control

Use Repo Control against canonical Vocab for:

- starting Git state;
- relevant Context discovery;
- baseline Snapshot;
- post-change Snapshot;
- Comparison where useful.

Do not modify Repo Control.

Do not access Photo Organizer.

Coding agent must not Stage or Commit M004.1 implementation.

---

# 5. First Task — Identify Exact External-Service Dependencies

Inspect the current canonical source and identify exactly how each existing feature works.

Map:

## Dictionary / definitions

Determine:

- service/provider;
- function(s);
- endpoint/API;
- credential requirement;
- current configuration source;
- current failure behavior.

## Translator

Determine:

- provider/library;
- whether an API credential is required;
- configuration source;
- current runtime behavior.

## Pronunciation / audio / TTS

Determine:

- provider/library;
- whether credentials are required;
- whether outbound Internet access is sufficient;
- runtime file/cache behavior;
- current error behavior.

## NLTK / WordNet

Determine:

- required corpus/resources;
- whether resources are baked into the image, downloaded at runtime, or absent;
- whether normal synonym behavior works in the current container.

Do not assume all external services use Google.

Document the actual providers and configuration requirements.

---

# 6. Diagnose the Current `Missing API Key` Failure

Trace the exact code path producing:

`Missing API Key! Check secrets.`

Determine:

- which feature emits it;
- exact secret/configuration key expected;
- whether it previously came from Streamlit `secrets.toml`, environment variables, or another source;
- whether containerization removed access to that configuration;
- whether the service itself is still valid.

Do not change product behavior until the actual cause is known.

---

# 7. Runtime Secret Architecture

Use the existing M004 external configuration model where practical.

Current operator-owned runtime configuration exists outside Git at:

`/home/chuck/.config/vocab-app/runtime.env`

Prefer adding external-service configuration there if the existing application can cleanly consume environment variables.

The repository may contain safe variable names/placeholders in:

`.env.example`

but never real values.

Required properties:

- secrets remain outside Git;
- secrets remain outside Docker image layers;
- secrets are not printed in logs;
- secrets are not written into closeout evidence;
- file remains mode `600`;
- normal Docker rebuild does not destroy the configuration.

If the current source only supports Streamlit secrets and changing to environment variables is a small bounded improvement, implement that compatibility cleanly.

Do not create a broad configuration framework.

---

# 8. Credential Operator Boundary

Do not invent or fabricate real credentials.

If an API credential is required and no safe value already exists on the server:

STOP at that specific operator boundary and report:

- provider;
- required configuration key;
- where the Product Owner should obtain/provide it;
- exact host file/configuration location expected.

Do not paste secret values into source, Compose, command lines, logs, or closeout.

If the Product Owner supplies a credential outside Git, continue validation without displaying its value.

---

# 9. Compose Integration

Update `compose.yaml` only as necessary to make the external runtime configuration available to `vocab-app`.

Prefer the existing external env-file model.

Do not:

- expose new ports for these APIs;
- add new containers unless the current feature actually requires one;
- add Google Sheets services;
- add PostgreSQL changes;
- modify unrelated stacks.

The PostgreSQL service and existing Vocab persistence architecture should remain unchanged.

---

# 10. Docker Image

Update the Docker image only if required for current external functionality.

Examples may include:

- required NLTK resource provisioning;
- required runtime package already declared but missing from image;
- safe directory/cache creation.

Do not add credentials to the image.

Do not make a large image redesign.

---

# 11. Dictionary Validation

After configuration is corrected, validate the existing Dictionary tab from the actual LAN application.

Use one or more ordinary test words.

Confirm:

- search completes;
- definition is returned where expected;
- existing part-of-speech/source behavior still renders;
- previous `Missing API Key` error is gone when configuration is valid;
- normal failure for an unknown word remains understandable.

Do not save test words to PostgreSQL unless deliberately validating the existing save flow.

If a controlled save is used, clean it up afterward.

---

# 12. Translator Validation

Validate the existing Translator tab.

Confirm:

- existing UI renders;
- a controlled translation request completes;
- no missing-runtime-config error occurs;
- current provider behavior remains unchanged.

Do not redesign translation behavior.

---

# 13. Pronunciation / Audio Validation

Validate the existing pronunciation/audio behavior.

Confirm:

- pronunciation/TTS request completes where currently supported;
- generated/playable audio reaches the browser;
- temporary audio behavior does not create uncontrolled persistent files;
- container permissions allow required temporary/cache writes.

Do not redesign pronunciation.

---

# 14. NLTK / Synonym Validation

Validate the current synonym path inside the operational container.

Confirm:

- required WordNet resources are available;
- no unexpected download/configuration failure occurs;
- existing five-result/set-order semantics remain unchanged;
- M003 characterization tests continue to pass.

Do not change synonym ordering for convenience.

---

# 15. Application-Level Validation

From the actual always-on service:

`http://192.168.1.173:8501`

validate the retained current features:

- Dictionary;
- Translator;
- Practice;
- pronunciation/audio where exposed;
- synonyms where exposed.

PostgreSQL Practice behavior must remain intact.

M004.1 should leave the application in a state where the major existing features intended for continued use actually function.

---

# 16. Tests

Before implementation, run the existing suite.

After implementation, run the complete suite again.

Current expected suite count should be at least the accepted M004 baseline.

Add only small tests necessary to validate:

- external configuration lookup;
- missing-key behavior;
- environment-variable precedence/compatibility if changed;
- bounded provider abstraction if introduced.

Do not create tests that make live Internet/API calls.

Use mocks/fakes for automated tests.

Manual runtime validation covers actual provider connectivity.

Run:

    python -m unittest discover -s tests -p 'test_*.py' -v

and appropriate compile/static checks:

    python -m py_compile app.py vocab_domain.py vocab_nlp.py vocab_persistence.py
    git diff --check

Include any newly added Python modules in compile validation.

---

# 17. Container Rebuild

After changes:

    docker compose \
      --env-file /home/chuck/.config/vocab-app/runtime.env \
      build
    
    docker compose \
      --env-file /home/chuck/.config/vocab-app/runtime.env \
      up -d
    
    docker compose \
      --env-file /home/chuck/.config/vocab-app/runtime.env \
      ps

Confirm:

- `vocab-db` remains healthy;
- `vocab-app` becomes healthy;
- PostgreSQL volume is unchanged;
- normal Vocab URL remains available.

Do not destroy the PostgreSQL named volume.

---

# 18. Security Validation

Confirm:

- no secret values are tracked by Git;
- no secret values are embedded in image layers;
- `.env.example` contains placeholders only;
- runtime configuration remains outside repository;
- `runtime.env` remains mode `600`;
- no secret values appear in bounded Docker logs;
- PostgreSQL remains internal-only;
- only existing Vocab LAN port remains published.

---

# 19. No Persistence Changes

Do not modify the PostgreSQL schema in M004.1 unless a genuine external-service requirement unexpectedly proves it necessary.

Do not change:

- Count semantics;
- practice selection;
- scoring;
- duplicate identity;
- database volume;
- current persistence interface.

This milestone is external-service runtime restoration only.

---

# 20. Explicit Non-Goals

Do not implement:

- Google Sheets persistence;
- Google data migration;
- multiple-choice practice;
- local-AI definitions;
- local-AI translation;
- local pronunciation/TTS replacement;
- permanent Streamlit replacement;
- public Internet access;
- authentication;
- TLS/reverse proxy;
- database redesign;
- unrelated UI redesign;
- new infrastructure services unless strictly required by an existing feature.

---

# 21. Stop Conditions

STOP and report if:

- the Dictionary provider/API is obsolete or unavailable;
- restoring the service requires unsafe secret handling;
- an external service now requires a paid/changed API contract not previously understood;
- the current feature cannot work without materially redesigning application behavior;
- runtime configuration would require embedding secrets;
- M004 PostgreSQL behavior regresses;
- unrelated container stacks would need modification;
- Repo Control and direct Git disagree;
- scope begins expanding into local AI or permanent UI replacement.

Do not substitute a new provider without Product Owner approval.

---

# 22. Required Closeout

Create exactly:

`milestones/004.1_external_service_runtime_configuration_and_functional_validation_closeout.md`

Include:

## 1. Executive conclusion

PASS / PARTIAL / STOP.

## 2. Starting Git state

Branch, HEAD, upstream parity, status.

## 3. Repo Control baseline

Snapshot and relevant Context evidence.

## 4. External-service map

For each current feature:

- feature;
- provider/library;
- credential/config requirement;
- runtime dependency.

## 5. Root cause

Exact reason for the `Missing API Key! Check secrets.` condition.

## 6. Runtime configuration solution

How configuration is supplied externally.

## 7. Files changed

Exact list.

## 8. Dictionary validation

Provider and observed result without exposing secret values.

## 9. Translator validation

Observed result.

## 10. Pronunciation/audio validation

Observed result.

## 11. NLTK/synonym validation

Observed result.

## 12. PostgreSQL regression validation

Confirm M004 persistence remains operational and unchanged.

## 13. Automated tests

Exact command/result.

## 14. Compile/static validation

Exact command/result.

## 15. Container rebuild

Exact command/result.

## 16. Container health

App/database status.

## 17. Desktop/mobile validation

Observed Product Owner or coder evidence.

## 18. Security validation

Secrets/image/Git/env/log checks.

## 19. Repo Control post-change evidence

Snapshot/Comparison IDs and limitations.

## 20. Remaining limitations

Only genuine limitations.

## 21. Recommendation

State whether M004.1 is ready for guarded Stage/Commit.

## 22. Next milestone

Normally:

`Vocab M005 — Multiple-Choice Practice`

## 23. Final Git state

Capture:

    git branch --show-current
    git rev-parse HEAD
    git status --short
    git rev-parse @{upstream} 2>/dev/null || true
    git rev-list --left-right --count HEAD...@{upstream} 2>/dev/null || true

---

# 23. Acceptance Criteria

M004.1 is PASS only if:

- the source of the missing API-key error is identified;
- required external runtime configuration is supplied safely;
- Dictionary lookup works from the containerized LAN application;
- Translator works at its existing functional boundary;
- pronunciation/audio works at its existing functional boundary;
- NLTK/synonym behavior works;
- PostgreSQL Practice behavior remains operational;
- no Google Sheets persistence is reintroduced;
- no credentials enter Git or the Docker image;
- containers remain healthy;
- existing tests pass;
- security/runtime configuration validation passes;
- implementation remains uncommitted for Product Owner review.

If a required external provider credential is unavailable, return PARTIAL with the exact operator boundary rather than fabricating success.

---

# 24. Post-M004.1 Direction

If M004.1 passes:

1. Product Owner reviews the closeout.
2. Guarded Stage through Repo Control.
3. Create matching Snapshot.
4. Guarded Commit.
5. Confirm Commit-aligned traceability.
6. Push `main`.
7. Rebuild the operational image from committed source.
8. Confirm external services still work.
9. Proceed to:

`Vocab M005 — Multiple-Choice Practice`

Do not open another infrastructure milestone unless this correction exposes a genuine new blocker.

---

# 25. Working Principles

> PostgreSQL is the database. Do not restore Google Sheets.

> Existing external word-enrichment services may remain external.

> Secrets belong in runtime configuration, not Git or image layers.

> Restore the application's intended current functionality before adding new features.

> Local-AI replacement is a future evaluation, not a corrective shortcut.

---

**End of `004.1_external_service_runtime_configuration_and_functional_validation_prompt.md`**
