# Vocab App Milestone 002.1 Closeout

## 1. Executive conclusion

Result: B — Adopt with bounded exceptions.

Observed fact: Repo Control is able to bind to the canonical Vocab repository and present a deterministic dashboard that matches direct Git evidence for branch, HEAD, workflow cleanliness, and upstream equality.

Observed fact: Repo Control Context is useful only for narrow lexical evidence discovery. In this canonical Vocab repository, the representative queries for `streamlit`, `google sheets`, `practice`, `container`, and `database` returned `no_matches`, while `synonym` returned a single-file, single-symbol result with one call relationship. That indicates the tool is useful for repository-structure navigation and deterministic file/symbol discovery, but it is not an architectural reasoning engine and it does not replace careful code reading.

Observed fact: Repo Control Snapshot and comparison workflows are operational and can create deterministic evidence for the canonical Vocab repository, but the comparison of identical snapshots produced very limited value because there was no real before/after state change.

Interpretation: Repo Control is ready to be used as a bounded workflow aid for future Vocab milestones, especially for Git-state evidence, repository binding, snapshotting, and comparison review. It should not be treated as a substitute for direct coder or architect reasoning.

Recommendation: adopt Repo Control with bounded exceptions and require direct Git evidence whenever a decision depends on repository truth.

Corrected continuation facts:

- Repo Control was not clean at the time of the original comparison; it had dirty working-tree files and an untracked `.venv/` directory.
- The Vocab M002.1 prompt was already committed at the recorded baseline, so the active continuation work is the closeout revision itself, not a new baseline state.
- The stage-preparation flow was executed in non-mutating mode and reported `NO TARGET GIT MUTATION PERFORMED`.

## 2. Starting Vocab evidence

Observed read-only direct Git evidence from `/home/chuck/projects/vocab-app`:

```bash
pwd
/home/chuck/projects/vocab-app

git branch --show-current
main

git rev-parse HEAD
a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c

git status --short
# no output

git remote -v
origin  https://github.com/CHHIV-Projects/vocab-app.git (fetch)
origin  https://github.com/CHHIV-Projects/vocab-app.git (push)

git rev-parse @{upstream}
a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c

git rev-list --left-right --count HEAD...@{upstream}
0       0

git log -5 --oneline --decorate
a3fcc8a (HEAD -> main, origin/main) Add Vocab M002.1 Repo Control integration baseline prompt
c0f79ab Close Vocab M002 architecture reconnaissance
9056811 Add Vocab M002 architecture reconnaissance prompt
d155722 Close Vocab M001 project control foundation
8b4e942 Establish Vocab project control foundation
```

Interpretation:

- Vocab is on canonical `main` and aligned with `origin/main`.
- The repository was clean at the direct baseline check.
- The Vocab M002.1 prompt file was already committed at that baseline; the closeout is the only untracked Vocab file being revised in place under this continuation.

## 3. Starting Repo Control evidence

Observed read-only direct Git evidence from `/home/chuck/projects/repo-control`:

```bash
pwd
/home/chuck/projects/repo-control

git branch --show-current
main

git rev-parse HEAD
859f9a651aee429fe20d55b8e5d236673480e7ff

git status --short
 M docs/008_guarded_staging_preparation_prompt.md
 M docs/009_browser_ui_foundation_and_read_only_console_prompt.md
?? .venv/

git rev-parse @{upstream}
859f9a651aee429fe20d55b8e5d236673480e7ff

git rev-list --left-right --count HEAD...@{upstream}
0       0
```

Interpretation:

- Repo Control is on its own `main` branch and has local upstream parity.
- Repo Control was not clean at the time of the relevant baseline: two modified doc files plus an untracked `.venv/` directory were present.
- This did not block the read-only Vocab integration check, but it demonstrates the need to verify direct Git truth before relying on Repo Control presentation.

## 4. Canonical repository binding

Observed facts:

- The Repo Control browser UI was started against `/home/chuck/projects/vocab-app`.
- The browser dashboard reported:
  - Active Repository: `vocab-app`
  - Path: `/home/chuck/projects/vocab-app`
  - Branch: `main`
  - Full SHA: `a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c`

Usability findings:

- Binding was clear and understandable.
- The dashboard makes the repository identity explicit.
- The repo-control UI is not yet a replacement for deep code inspection, but it is acceptable as a deterministic Git-state and repository-context entry point for Vocab work.

## 5. Dashboard / Git State comparison

Observed direct-vs-Repo Control comparison:

- Direct Git branch: `main` -> Repo Control dashboard: `main` -> MATCH
- Direct Git HEAD: `a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c` -> Repo Control dashboard: full SHA `a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c` -> MATCH
- Direct Git working tree: clean -> Repo Control dashboard: `Workflow: clean` and `Staged/Unstaged/Untracked = 0` -> MATCH
- Direct Git upstream relation: `origin/main` equal -> Repo Control dashboard: `Local relation = equal` -> MATCH
- Ahead/behind: `0 / 0` -> Repo Control dashboard: `Ahead = 0`, `Behind = 0` -> MATCH

Interpretation:

- Repo Control dashboard correctly reflects the canonical Vocab repository state for the direct baseline evidence.
- This is the strongest evidence that the current Repo Control workflow can be used as a repository-status front end for Vocab.

## 8. Required bounded Context follow-up evidence

The addendum required focused follow-up queries to distinguish product-language failure from implementation-discovery failure.

| Query | Match status | Seed count | Selected files | Selected symbols | Relationships | app.py surfaced | Result class |
|---|---|---:|---:|---:|---:|---|---|
| `gspread` | no_matches | 0 | 0 | 0 | 0 | no | no useful result |
| `worksheet` | no_matches | 0 | 0 | 0 | 0 | no | no useful result |
| `get_sheet` | matched | 5 | 1 | 6 | 4 | yes | useful implementation-level lookup |
| `quiz` | no_matches | 0 | 0 | 0 | 0 | no | no useful result |
| `update_score` | matched | 1 | 1 | 2 | 1 | yes | useful implementation-level lookup |
| `session_state` | no_matches | 0 | 0 | 0 | 0 | no | no useful result |
| `get_synonyms_nltk` | matched | 5 | 1 | 6 | 4 | yes | useful implementation-level lookup |

Interpretation:

- Product-language terms like `google sheets`, `practice`, and `streamlit` did not surface the implementation under the current deterministic Context engine.
- Known implementation symbols like `get_sheet`, `update_score`, and `get_synonyms_nltk` did resolve and surfaced `app.py`.
- This means Context is not a reliable product-to-code navigator for ordinary Vocab domain language, but it can still help a coder who already knows the implementation vocabulary.

Classifications:

- A — Product-language vocabulary gap: yes.
- B — Known-symbol discovery works: partially.
- C — Known-symbol discovery weakness: no evidence for the tested symbols.
- D — Expected implementation absence: yes for `container` and `database` as appropriate.
- E — Architectural inference boundary: yes.
- F — Potential core Context defect: not supported by the evidence.

## 7. Context — Streamlit

Query: `streamlit`

Observed result:

- `match_status: no_matches`
- `seed_count: 0`
- `selected_file_count: 0`
- `selected_symbol_count: 0`

M002 comparison:

- M002 found direct Streamlit coupling throughout `app.py`.
- The Streamlit coupling is real and highly relevant to the product architecture.

Usefulness rating: LOW

Interpretation:

- The query is not semantically rich enough for the current lexical Repo Control context engine.
- This does not mean the coupling is absent; it means Context did not surface it under this exact lexical query.
- This is a limitation of the current deterministic context engine, not proof that the architecture is clean.

## 9. Context — Google Sheets persistence

Query: `google sheets`

Observed result:

- `match_status: no_matches`
- `seed_count: 0`

M002 comparison:

- M002 identified direct Google Sheets integration and authentication logic inside `app.py`.

Usefulness rating: LOW

Interpretation:

- A direct lexical query for the product-level concept did not match the canonical code.
- The implementation is there, but the term selection is not surfaced by the deterministic context pack in a way that helps a coder reason about the persistence boundary.
- This is a real usability issue for future architecture reconnaissance.

## 10. Context — quiz/test behavior

Query: `practice`

Observed result:

- `match_status: no_matches`
- `seed_count: 0`

M002 comparison:

- M002 identified the actual practice/flashcard workflow inside `app.py`.
- The flow is a flashcard-style quiz, not a multiple-choice engine.

Usefulness rating: LOW

Interpretation:

- Repo Control Context is not yet a reliable way to find the real quiz/test workflow by product-language query alone.
- This is a meaningful limitation for a future multiple-choice milestone.

## 11. Context — synonyms

Query: `synonym`

Observed result:

- `match_status: matched`
- `selected_file_count: 1`
- `selected_symbol_count: 2`
- One relationship: `app.py:get_mw_data` calls `app.py:get_synonyms_nltk`

M002 comparison:

- M002 identified the same function and its role in definition lookup.
- Historical testbed references also contain synonym normalization logic in `vocab_utils.py` / `synonym_policy.py`.

Usefulness rating: MEDIUM

Interpretation:

- This is the best of the representative queries because it hits a real symbol name in the canonical code.
- It is still limited: it provides a call relationship but does not explain the historical normalization logic or the broader product implication.
- It is useful as a starting point, not as a complete architecture map.

## 12. Context — container/runtime

Query: `container`

Observed result:

- `match_status: no_matches`

M002 comparison:

- M002 established that there is no canonical Dockerfile or Compose file, and that containerization work remains a future requirement.

Usefulness rating: LOW

Interpretation:

- This is an expected limitation rather than a defect. There is simply no runtime/container implementation in the current repo to match.
- Repo Control should not be expected to infer future architecture from a missing implementation.

## 13. Context — database/persistence

Query: `database`

Observed result:

- `match_status: no_matches`

M002 comparison:

- M002 identified the actual persistence boundary: Google Sheets in `app.py`, not a database layer.

Usefulness rating: LOW

Interpretation:

- This is a real limitation of the current query engine: no database layer exists yet, and the product concept does not map directly to a lexical match.
- The result is not a contradiction; it is evidence that the canonical repo does not yet contain the future database abstraction.

## 14. Context comparison matrix

| Subject | Product-language query | Implementation-level control/query | Repo Control result | Agreement with direct evidence | Overall assessment |
|---|---|---|---|---|---|
| Google Sheets | `google sheets` | `gspread`, `worksheet`, `get_sheet` | product-language query: no_matches; implementation-level control: `get_sheet` matched, `gspread` and `worksheet` no_matches | direct evidence shows Google Sheets persistence in `app.py`; Context only surfaced the implementation when the query approached the actual symbol form | product-language discovery is weak; implementation-level lookup is materially better |
| Practice / quiz | `practice` | `quiz`, `update_score` | product-language query: no_matches; implementation-level control: `update_score` matched, `quiz` no_matches | direct evidence shows practice/score logic in `app.py`; Context did not surface the domain-level term but did find the canonical symbol | product-language discovery is weak; symbol-driven discovery works for the tested canonical function |
| Streamlit | `streamlit` | `session_state` | `streamlit`: no_matches; `session_state`: no_matches | direct evidence shows Streamlit use throughout `app.py`; Context did not find either the domain term or the nearby implementation term | this is a lexical indexing limitation, not evidence of a missing app implementation |
| Synonyms | `synonym` | `get_synonyms_nltk` | `synonym`: matched; `get_synonyms_nltk`: matched | direct evidence confirms the function role in `app.py` | the strongest tested case; symbol proximity is the key factor |
| Container/runtime | `container` | none | `no_matches` | direct evidence confirms there is no container baseline in the repo | expected absence, not a defect |
| Database/persistence | `database` | none | `no_matches` | direct evidence confirms persistence is via Google Sheets, not a database layer | expected mismatch under current repo architecture |

This matrix replaces the original no-match-only view and captures the real distinction: ordinary product-language discovery is weak, while known canonical symbol discovery is usable for the tested symbols.

## 15. Duplicate/noise evaluation

Observed facts:

- The Repo Control Context engine produced a clean, bounded result for the synonym query.
- The no-match results for other conceptual queries are not necessarily duplicates; they are simply non-matches.
- The context output is not noisy in the sense of obvious repeated duplicates, but it is limited and does not surface the same depth of architectural evidence that the independent M002 reconnaissance did.

Interpretation:

- The deterministic evidence quality is acceptable for narrow lexical lookup.
- For architecture questions, the context engine still needs human-coded architectural interpretation.

## 16. Snapshot evaluation

Observed facts:

- Repo Control created a snapshot for canonical Vocab:
  - `snap--72ce00b7c17ade12`
- A second snapshot request reused the existing snapshot (`reused_existing: True`).

Evaluation:

- Snapshot creation is operational and deterministic.
- The output is useful as a stable repository-state reference.
- The snapshot ID is understandable and could be used in future comparison or review evidence.
- The snapshot is not a replacement for direct Git state; it is a structural evidence artifact.

## 17. Comparison evaluation

Observed facts:

- Repo Control created a comparison for the same snapshot against itself:
  - `cmp--147b8d029c2bde49`

Evaluation:

- The comparison was valid in a technical sense.
- The result had limited product value because there was no real before/after change.
- This demonstrates the workflow is operational but not yet rich enough to substitute for a real meaningful diff review.

Usefulness rating: MEDIUM for workflow mechanics, LOW for architectural insight in this specific state.

## 18. Product Owner Review evaluation

The Repo Control product review capability available in this environment is the deterministic evidence console and the local AI review screen, not a distinct formal Product Owner approval artifact. The clearest meaningful evidence for review was the canonical Vocab repo state plus the comparison `cmp--147b8d029c2bde49`.

Deterministic evidence reviewed:

- active repository: `vocab-app`
- path: `/home/chuck/projects/vocab-app`
- branch: `main`
- HEAD: `a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c`
- workflow: clean
- upstream: `origin/main` equal
- comparison: `cmp--147b8d029c2bde49`
- no structural changes detected between before and after snapshots

Review result:

- the system presents deterministic evidence in a human-readable form;
- the key conclusion is that there is no real before/after source change to review here;
- the evidence supports repository-state verification, not product or implementation change approval;
- there is no actual Product Owner approval or execution artifact in the reviewed UI path.

Interpretation:

- the review capability is useful as a human-readable evidence layer;
- it is not a substitute for direct source understanding or for a formal approval gate.

## 19. Local-AI assessment evaluation

Observed fact:

- the `AI Review` screen is available under the Repo Control evidence console;
- local GPT-OSS was run against comparison `cmp--147b8d029c2bde49` and produced `Analysis ID: ana--53aa95629229123d`;
- the advisory output stated: "No structural changes were detected between the before and after snapshots."
- the screen also explicitly states: "AI analysis is advisory. Deterministic repository evidence remains authoritative."

Deterministic evidence supplied to the review:

- `Comparison ID: cmp--147b8d029c2bde49`
- same branch and same HEAD before/after
- repo state clean and equal to upstream
- no structural diffs

Advisory output:

- Summary: no structural changes detected between the before and after snapshots
- Review Signals: none
- Questions for Human Review: none

Assessment:

- the local AI interpretation added a useful summary, but only at the margin;
- it did not add any unsupported architectural claim beyond the evidence it was given;
- it did not detect any Git or safety issue because there was no source mutation to evaluate;
- AI remains non-authoritative and serves only as an advisory summary layer.

If there is no meaningful supported path, document that exact reason:

- there is a meaningful supported path in this repoctl environment, but it is strictly advisory and only meaningful when a deterministic comparison exists;
- this review did not expose any formal approval authority or a richer Product Owner workflow beyond summary generation.

## 20. Guarded Stage evidence

The addendum required a real guarded Stage test, but not execution until Product Owner approval of the exact Stage Plan. We ran the preparation step:

```bash
repoctl milestone prepare-stage --repository /home/chuck/projects/vocab-app --all
```

Observed output:

- Repository: `/home/chuck/projects/vocab-app`
- Branch: `main`
- HEAD: `a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c`
- Changes proposed for staging: 1
- Candidate path: `milestones/002.1_repo_control_integration_baseline_and_m002_evidence_comparison_closeout.md`
- Plan: `stage-plan--aee14f6fd06d1aa6`
- Status: `NO TARGET GIT MUTATION PERFORMED`

This is the exact non-mutating stage-prep state. Stage execution itself was intentionally not performed because the workflow requires explicit Product Owner approval of the exact plan before mutation.

## 21. Role utility map

- Product Owner: should use Dashboard, Snapshot, Comparison, and Product Owner Review for meaningful milestone decisions.
- Architect: should use Context and historical repo evidence to interpret architecture questions, but not rely on Context as a complete architecture map.
- Coder: should use Dashboard/Git State and narrow Context lookups as a starting point, then read code directly for the actual implementation and relationships.
- Multiple roles: Snapshot and Comparison are most valuable when they represent real before/after states.

## 22. Coder workflow recommendation

Recommended standard usage for future Vocab milestones:

- before implementation: use Dashboard/Git State to confirm repo truth;
- use Context for a narrow file/symbol search only;
- use Snapshot when a baseline or milestone boundary is meaningful;
- do not rely on Context to replace actual code reading;
- after implementation: run direct Git and Repo Control state checks; preserve Comparison evidence when there is a real diff to compare.

This is the minimum useful workflow should be added to future Vocab milestones without adding ceremony.

## 23. Architect workflow recommendation

Recommended evidence for the Architect:

- direct Git state;
- Context results for the specific architectural question under review;
- Snapshot IDs and Comparison summaries when a state change exists;
- Product Owner Review output if a milestone is review-heavy.

The Architect should not be expected to rely on raw Repo Control outputs as a full architectural review substitute.

## 24. Repo Control vs coder boundary

Observed boundary:

- Repo Control is best as deterministic, repository-state and structural evidence tooling.
- The coder remains responsible for understanding actual implementation behavior and product architecture.
- The architect still interprets architecture and restructure risk.
- The Product Owner remains responsible for product approval and guarded mutation decisions.

This supports the working model:

`Repo Control finds and preserves deterministic evidence`

`Coder understands and changes implementation`

`Architect interprets architecture and plans milestones`

`Product Owner approves product direction and guarded mutation`

## 25. Guarded Stage evidence

No Stage operation was executed.

Reason:

- The prompt requires Product Owner approval before Stage usage.
- As of this validation, there was no explicit Product Owner approval tied to an exact immutable Stage Plan.
- The repo remained in a clean state and the working tree was not mutated.

This is the correct safety boundary to preserve.

## 26. Matching Snapshot / commit eligibility evidence

No commit plan or commit execution was created.

Reason:

- The repo was clean.
- There was no approved mutation plan.
- The milestone’s real documentation artifact was created as a closeout file without staging or commit, which is consistent with the requirement to keep Vocab application code unchanged.

## 27. Guarded Commit evidence

No Guarded Commit was executed.

Reason:

- no approved Stage/Commit plan was reviewed or approved;
- no Product Owner authorization was obtained;
- pushing was explicitly out of scope.

## 28. Push boundary

Push was not performed.

Status:

- direct Git upstream relation remained equal;
- no push authority was exercised as part of this milestone.

## 29. Gap classification

A — Expected limitation

- Conceptual queries such as `streamlit`, `google sheets`, `practice`, `container`, and `database` produced no matches or low-value results because the current deterministic context engine is lexical and not architectural.

B — Usability issue

- The Context engine is useful for a narrow lexical symbol search, but it is difficult to use as a complete architecture-discovery tool for product-level concepts.

C — Evidence-quality issue

- Some conceptual queries are underpowered and do not surface the same facts that M002 found from direct code reading.

D — Core correctness defect

- None observed. Repo Control dashboard and direct Git state agreed.

E — Missing future capability

- Real Vocab workflow needs such as deployment SHA, runtime health, and later deployment evidence are still future capabilities, not current M002.1 defects.

## 30. M002 vs M002.1 comparison

What both approaches found:

- The canonical repo is `main` and aligned with `origin/main`.
- The app is still a single-file Streamlit app with a direct Google Sheets persistence boundary.
- The canonical repo has no obvious test suite and no container baseline.

What coder reconnaissance found that Repo Control did not:

- the actual full runtime flow in `app.py`;
- the coupling between UI, persistence, secret access, audio, NLP, and quiz logic;
- the significance of the flashcard/score flow and its non-multiple-choice nature;
- the existence of relevant historical testbed ideas in archive/test branches.

What Repo Control surfaced more deterministically or clearly:

- direct Git state; repository binding; snapshot IDs; workflow cleanliness; upstream equality.

Did Repo Control expose evidence that would have improved M002?

- It provided deterministic repository-state evidence and a clean dashboard, but it did not materially improve the core architecture conclusions of M002.

Would using Repo Control during M002 have materially changed the conclusions?

- It would likely have improved the repository-state and baseline evidence, but not the architecture conclusions. The M002 findings are still sound.

## 31. Workflow adoption decision

Decision: B — Adopt with bounded exceptions.

Reasoning:

- Dashboard/Git State is useful and reliable.
- Snapshot and comparison are usable for deterministic evidence capture.
- Context is useful for a narrow lexical symbol search but not as a complete architecture-discovery mechanism.
- Some later workflows, especially Product Owner review and Stage/Commit, require deliberate approval and must remain guarded.
- The revised evidence does not materially change the B decision. It strengthens the bounded-exception wording: Repo Control is appropriate as a deterministic evidence layer, not as a product-language architecture navigator.

## 32. Minimum standard Vocab Repo Control workflow

Recommended minimum workflow for future Vocab milestones:

- Before implementation: use Dashboard/Git State; verify repository binding and branch status.
- Use Context only for narrow and specific code discovery questions.
- Create a meaningful Snapshot at milestone boundaries when evidence matters.
- Use Comparison only when there is a real before/after state to compare.
- Use Product Owner Review only in the exact review context that requires human approval.
- Use Stage and Commit only under explicit Product Owner approval and with exact immutable plan review.

Optional usage:

- local AI analysis, only when a comparison exists and a human reviewer wants an advisory summary.

## 33. Future runtime/deployment integration hooks

Explicitly deferred.

This milestone remained in the source-control and repository-evidence layer only. Future work will need distinct runtime/deployment evidence for:

- image identity;
- source SHA vs image SHA;
- running container identity;
- deployed revision;
- runtime health;
- deployment comparison.

These are future opportunities, not M002.1 defects.

## 34. Recommended next Vocab milestone

Recommended next milestone: M003 — Bounded Refactor and Characterization Tests

Reason:

- The current architecture is monolithic and tightly coupled.
- The M002 conclusions support a bounded refactor before product changes like multiple choice and database migration.
- This milestone would reduce implementation risk without changing the intended product direction.

## 35. Separate Repo Control milestone recommendations

A separate generalized Repo Control reconnaissance milestone is justified by the current evidence, but it should be scoped to deterministic product-language/source-text-to-symbol discovery and remain separate from Vocab implementation work.

Why a separate milestone is justified:

- product-language discovery is materially weaker than implementation-symbol discovery;
- the tested canonical symbols do resolve, which shows the underlying lexical layer can work when terms approach real source identifiers;
- the issue is not a Git or safety defect but a discovery-quality gap in mapping ordinary product language to code-level vocabulary;
- the improvement target is clear and bounded: improve deterministic lexical coverage and bridging from source text to canonical symbol names without introducing semantic AI dependence.

Recommended scope for that separate Repo Control milestone:

- improve product-to-symbol lexical matching for common domain terms,
- improve deterministic source-text indexing quality,
- keep the improvement in the repository-control plane and not within Vocab implementation,
- evaluate whether small lexical or index improvements materially improve navigation for future repo work,
- do not prescribe semantic AI search unless the evidence shows a genuine gap beyond lexical coverage.

This is not a Vocab implementation milestone and does not change the current adoption decision for Vocab itself.

## 36. Limitations / unresolved questions

- No Product Owner approval for Stage/Commit was obtained during this validation.
- No live deployment or runtime validation was performed.
- No browser-review workflow beyond the read-only Dashboard and bound repo page was executed under approval.
- Context is still lexical, not semantic, so its architectural usefulness remains bounded.

## 37. Final Vocab repository state

Observed final Vocab state after the M002.1 validation and closeout creation:

```bash
cd /home/chuck/projects/vocab-app

git branch --show-current
main

git rev-parse HEAD
a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c

git status --short
?? milestones/002.1_repo_control_integration_baseline_and_m002_evidence_comparison_closeout.md

git rev-parse @{upstream} 2>/dev/null || true
a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c

git rev-list --left-right --count HEAD...@{upstream} 2>/dev/null || true
0       0

git log -3 --oneline --decorate
a3fcc8a (HEAD -> main, origin/main) Add Vocab M002.1 Repo Control integration baseline prompt
c0f79ab Close Vocab M002 architecture reconnaissance
9056811 Add Vocab M002 architecture reconnaissance prompt
```

## 38. Final Repo Control repository state

Observed read-only Repo Control state:

```bash
cd /home/chuck/projects/repo-control

git branch --show-current
main

git rev-parse HEAD
859f9a651aee429fe20d55b8e5d236673480e7ff

git status --short
 M docs/008_guarded_staging_preparation_prompt.md
 M docs/009_browser_ui_foundation_and_read_only_console_prompt.md
?? .venv/
```

This milestone preserved read-only evidence and did not mutate Vocab application code, Repo Control source, Git history, runtime state, or database state.

## 39. Final Context conclusion

Can a coder begin with ordinary product terminology and navigate to the relevant implementation through Repo Control, or must the coder already know implementation-level names?

The current evidence shows that a coder must already know implementation-level names or read the source directly. Ordinary product/domain language such as `google sheets`, `practice`, `streamlit`, `container`, and `database` is weakly discoverable in the deterministic Context layer. The tested canonical symbols `get_sheet`, `update_score`, and `get_synonyms_nltk` did resolve, while arbitrary code/import/attribute terms such as `gspread` and `session_state` did not. This is not evidence of a core Git or safety defect; it is evidence of a lexical indexing and product-to-symbol mapping gap. Repo Control is useful for deterministic repository evidence and narrow symbol lookup, but it is not a reliable product-language-to-implementation navigator in the present state.

## 40. New stage-plan evidence

The prior Stage Plan concept was intentionally not executed and is stale relative to the final closeout revision. The new stage-preparation evidence was generated against the final closeout and remains non-mutating:

- Plan ID: `stage-plan--49decb092b836edd`
- Candidate path: `milestones/002.1_repo_control_integration_baseline_and_m002_evidence_comparison_closeout.md`
- Repository: `/home/chuck/projects/vocab-app`
- Branch: `main`
- HEAD: `a3fcc8a7bd1e25676d9dbe11de5c644a629a9c1c`
- Status: `NO TARGET GIT MUTATION PERFORMED`

This exact plan must still receive explicit Product Owner approval before any Stage execution.
