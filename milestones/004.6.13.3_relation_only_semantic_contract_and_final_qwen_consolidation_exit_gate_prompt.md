# M004.6.13.3 — Relation-Only Semantic Contract & Final Qwen Consolidation Exit Gate

**Prompt filename:** `milestones/004.6.13.3_relation_only_semantic_contract_and_final_qwen_consolidation_exit_gate_prompt.md`

**Required closeout filename:** `milestones/004.6.13.3_relation_only_semantic_contract_and_final_qwen_consolidation_exit_gate_closeout.md`

**Project:** Vocab App
**Repository:** `/home/chuck/projects/vocab-app`
**Primary branch:** `main`

**Milestone type:** Final bounded Qwen consolidation experiment + conditional production-contract finalization
**Parent arc:** M004.6.13 / M004.6.13.1 / M004.6.13.2
**This is the FINAL Qwen3:8B automatic-consolidation calibration milestone for v1.**
**Additional prompt variants authorized:** NO
**Additional model comparison authorized:** NO
**Seed tuning authorized:** NO
**Embedding/screening experiments authorized:** NO
**Relation-only contract authorized:** YES
**Conditional production v3 implementation authorized:** YES, only if the frozen exit gate passes
**Learner UI activation authorized:** NO
**Database migration authorized:** NO
**Git Stage/Commit/Push/Tag/Merge authorized:** NO

# 1. Objective

M004.6.13.2 established that neither existing structurally hardened configuration is suitable.

Configuration B:

historical semantic prompt
+
hardened `oneOf` decision/reason schema

produced useful MERGE recall but excessive KEEP→MERGE disagreement.

Configuration D:

current v2 prompt
+
hardened `oneOf` schema

eliminated false merges on the calibration set but collapsed to 100% `keep_separate`, with 0% MERGE recall.

The historical failure pattern also revealed an architectural clue:

Qwen repeatedly produced outputs such as:

`merge / specialized_subsense`

The `reason` frequently appeared semantically plausible even when the accompanying action was inconsistent.

M004.6.13.3 therefore tests one final simplification:

**Qwen will classify only the semantic relationship between two source senses.**

Qwen will NOT decide:

- MERGE;
- KEEP-SEPARATE;
- UNCERTAIN.

Python will derive that action deterministically from the semantic relationship.

This more closely follows the governing architecture:

**AI decides semantics. Code owns policy, topology, completeness, identity, persistence, and validation.**

# 2. Final Anti-Doom-Loop Boundary

This milestone is deliberately finite.

There is exactly ONE new semantic contract candidate.

Do NOT create:

- multiple prompt variants;
- prompt A/B/C/D/E experiments;
- alternate schemas;
- alternate seeds;
- alternate temperatures;
- alternate models;
- few-shot variants;
- chain-of-thought variants;
- additional calibration milestones automatically.

At the end of M004.6.13.3 there are only two allowed architectural outcomes:

## Outcome GO

The relation-only candidate is sufficiently useful and safe to become the v3 consolidation contract.

Proceed afterward to learner-facing transparent evaluation.

## Outcome NO-GO

Qwen3:8B is not sufficiently reliable for automatic sense merging under this architecture for the current product version.

Stop automatic semantic merging research for v1.

Preserve source senses conservatively and continue the product without automatic consolidation, while retaining the architecture so a better model/approach can be substituted later.

There is NO automatic M004.6.13.4 prompt-calibration experiment after a NO-GO.

# 3. Important Interpretation of the Reference Dataset

The M004.6.6 adjudicated dataset is NOT an external linguistic gold standard.

It is a **project-policy reference set** created from real Wiktionary source-sense pairs and reviewed/adjudicated within this project.

Therefore:

- benchmark disagreement does not automatically mean Qwen is linguistically wrong;
- agreement metrics are still useful for detecting severe over-merging, over-splitting, or decision collapse;
- the purpose of this exit gate is to determine whether the relation-only candidate is plausible enough for controlled product use;
- final product quality will ultimately be evaluated in the learner UI with the original source senses visible.

Do NOT describe reference disagreements as incontrovertible linguistic errors.

Use terminology such as:

- reference agreement;
- KEEP→MERGE disagreement;
- MERGE→KEEP disagreement;
- project-policy disagreement.

Avoid overstating "ground truth."

# 4. Why Relation-Only

The current model-facing task asks Qwen to make two coupled decisions:

1. semantic relationship;
2. action.

Example historical output:

`merge / specialized_subsense`

may indicate that Qwen correctly recognized:

`specialized_subsense`

but incorrectly paired it with:

`merge`.

The relation-only contract removes the second model decision entirely.

Model:

source sense A
+
source sense B
    ->
semantic relation

Python:

semantic relation
    ->
deterministic action

The model never emits MERGE / KEEP-SEPARATE / UNCERTAIN.

# 5. The Seven Semantic Relations

The model must choose exactly ONE value:

1. `paraphrase_or_duplicate`
2. `contextual_variant`
3. `specialized_subsense`
4. `broader_or_narrower`
5. `related_but_distinct`
6. `materially_different`
7. `insufficient_evidence`

No other semantic category is authorized.

# 6. Deterministic Action Mapping

Python owns the action policy.

The exact mapping is:

`paraphrase_or_duplicate`
    ->
`merge`

`contextual_variant`
    ->
`merge`

`specialized_subsense`
    ->
`keep_separate`

`broader_or_narrower`
    ->
`keep_separate`

`related_but_distinct`
    ->
`keep_separate`

`materially_different`
    ->
`keep_separate`

`insufficient_evidence`
    ->
`uncertain`

This mapping is deterministic and closed.

Qwen must not be shown or asked to emit the resulting action.

# 7. Relation Definitions

Use the following semantic meanings.

These are classifier definitions, NOT action instructions.

## `paraphrase_or_duplicate`

The two senses express effectively the same lexical meaning. Differences are primarily wording, phrasing, or redundant dictionary decomposition.

## `contextual_variant`

The two senses express the same underlying lexical meaning in different ordinary contexts or applications. The contextual difference does not create a materially different lexical concept.

## `specialized_subsense`

The senses share a semantic core, but one carries a specialized domain, scope, technical meaning, or usage distinction that represents a meaningful lexical specialization.

## `broader_or_narrower`

One sense semantically contains, generalizes, specializes, or is a subtype/instance of the other rather than expressing the same scope.

## `related_but_distinct`

The senses are semantically related but represent meaningfully different concepts or uses.

## `materially_different`

The senses represent substantively different lexical meanings.

## `insufficient_evidence`

The available evidence is insufficient to classify the relationship confidently.

# 8. Exact Model-Facing Task

The new candidate prompt must ask ONLY:

> Classify the semantic relationship between these two dictionary senses. Return exactly one relation from the allowed relation set.

The prompt may include the definitions from Section 7.

Do NOT tell Qwen:

- which relations cause merging;
- which relations cause separation;
- which action is safer;
- to prefer MERGE;
- to prefer KEEP-SEPARATE;
- that false splits are safer;
- that false merges are dangerous.

Those are product-policy concerns owned by deterministic code.

The model's task is semantic classification only.

# 9. Structured Output

Use the smallest practical closed schema:

{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "relation": {
      "type": "string",
      "enum": [
        "paraphrase_or_duplicate",
        "contextual_variant",
        "specialized_subsense",
        "broader_or_narrower",
        "related_but_distinct",
        "materially_different",
        "insufficient_evidence"
      ]
    }
  },
  "required": ["relation"]
}

Do not use `oneOf`.

Do not ask the model to emit separate `decision` and `reason` fields.

# 10. Runtime Lock

Use exactly:

- model: `qwen3:8b`
- digest:
  `500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`
- `think=false`
- `temperature=0`
- `seed=5100`
- `num_predict=256`
- `num_ctx=16384`

No tuning.

If model digest differs:

STOP.

# 11. Current Uncommitted M004.6.13 Work

M004.6.13 production changes remain intentionally uncommitted.

Expected modified files include:

- `vocab_consolidation.py`
- `vocab_consolidation_provider.py`
- `tests/test_vocab_consolidation.py`
- `tests/test_vocab_consolidation_provider.py`
- `tests/test_m004612_postgres_isolated.py`
- M004.6.13 closeout

Do not discard these changes at milestone start.

They contain useful contract-versioning and historical-compatibility work.

M004.6.13.3 may evolve them into v3 ONLY after the benchmark exit gate passes.

# 12. Mandatory Git Boundary

Coder must NOT:

- Stage;
- `git add`;
- Commit;
- Push;
- Tag;
- Merge;
- Rebase;
- Reset;
- Restore;
- Stash;
- create/delete branches;
- rewrite history.

Product Owner controls Git publication.

Read-only Git commands are allowed.

# 13. Required Context

Read:

1. `docs/context/vocab_app_project_context_v2.md`
2. `docs/context/vocab_app_architecture_v2.md`
3. `docs/context/project_workflow_v2.md`
4. `coding_agent_rules_v2.md`
5. M004.6.4 final closeout
6. M004.6.6 final closeout
7. M004.6.9 final closeout
8. M004.6.11 final closeout
9. M004.6.12 final closeout
10. M004.6.13 prompt and latest closeout
11. M004.6.13.1 prompt and accepted PARTIAL closeout
12. M004.6.13.2 prompt and accepted PASS closeout
13. `vocab_consolidation.py`
14. `vocab_consolidation_provider.py`
15. `vocab_consolidation_orchestrator.py`
16. `vocab_consolidation_workflow.py`
17. final M004.6.6 project-policy reference artifact
18. M004.6.13.2 benchmark harness/results
19. this prompt

# 14. Preflight

Record:

- repository;
- branch;
- HEAD;
- upstream;
- literal `git status --short`;
- Ollama version;
- Qwen digest.

Identify:

- current provisional v2 production changes;
- committed M004.6.13.2 benchmark files;
- any unrelated worktree changes.

If unexplained unrelated changes exist:

STOP.

# 15. Protect Current Work Before Benchmark

Before benchmark experimentation, record SHA256 hashes for:

- `vocab_consolidation.py`
- `vocab_consolidation_provider.py`
- `tests/test_vocab_consolidation.py`
- `tests/test_vocab_consolidation_provider.py`
- `tests/test_m004612_postgres_isolated.py`

Phase A benchmark work must not modify those files.

Only if the exit gate passes may Phase B modify them to implement v3.

# 16. Phase A — Benchmark-Only Candidate

First implement the relation-only candidate in benchmark-only code.

Do NOT change production provider/domain code yet.

Recommended additive harness:

`tools/benchmarks/m0046133_relation_only_exit_gate.py`

Recommended tests:

`tests/test_m0046133_relation_only_exit_gate.py`

The benchmark harness must use the current production evidence projection and model request conventions wherever applicable.

# 17. Untouched Holdout Requirement

Do NOT reuse the 16 scored pairs from M004.6.13.2 as the primary M004.6.13.3 exit-gate sample.

Those pairs have already influenced architectural decisions.

Use the final accepted M004.6.6 project-policy reference set and EXCLUDE all pair IDs in the M004.6.13.2 frozen selection.

# 18. Frozen Exit-Gate Sample

From the remaining M004.6.6 pairs select:

- **20 reference MERGE pairs**
- **40 reference KEEP-SEPARATE pairs**

Also include all remaining available reference UNCERTAIN pairs, up to the known set of 6, as an auxiliary group.

Primary scored set:

60 pairs.

Auxiliary uncertainty set:

up to 6 pairs.

Do not substitute already-used M004.6.13.2 scored pairs.

# 19. Deterministic Selection

Within each reference class, select by ascending:

`SHA256("m004.6.13.3-final-exit-v1|" + stable_pair_id)`

Select the lowest hashes needed after excluding M004.6.13.2 pairs.

No cherry-picking.

Record:

- pair ID;
- lemma;
- POS;
- reference decision;
- reference reason if available;
- sense IDs;
- source definitions;
- selection hash.

# 20. Freeze Before Inference

Before the first Qwen call, create:

`selection.json`

under:

`/home/chuck/.local/share/vocab-app/benchmarks/m004.6.13.3/<timestamp>/`

Record SHA256.

Print/report:

- 20 MERGE pair IDs;
- 40 KEEP pair IDs;
- auxiliary UNCERTAIN IDs.

Once inference begins:

DO NOT modify the sample.

# 21. One Candidate Only

Every selected pair is evaluated exactly once with the relation-only candidate.

No comparison prompt.

No alternate schema.

No seed variation.

No retry for semantic disagreement.

# 22. Infrastructure Retry

One exact retry is permitted only for a true infrastructure failure such as:

- HTTP failure;
- Ollama connection loss;
- timeout.

The retry must use byte-identical semantic input/options.

Record it.

Do not retry because you dislike the semantic result.

# 23. Benchmark Call Budget

Expected:

60 primary
+
up to 6 auxiliary uncertain
=
up to 66 semantic calls.

Allow up to 6 infrastructure-only retries.

Phase A maximum:

**72 Qwen calls.**

Do not exceed this.

# 24. Scoring Model Output

For each call record:

- pair ID;
- reference decision;
- returned relation;
- derived deterministic action;
- parse/schema validity;
- whether derived action agrees with reference decision;
- source definitions.

Primary evaluation is ACTION-LEVEL agreement.

Do not require exact relation/reason agreement where the project reference reason is absent or subjective.

# 25. Terminology

Because the reference is a project-policy reference rather than external ground truth, report:

- MERGE reference agreement;
- KEEP reference agreement;
- KEEP→MERGE disagreement;
- MERGE→KEEP disagreement;
- MERGE→UNCERTAIN disagreement;
- KEEP→UNCERTAIN disagreement.

Do not automatically describe every disagreement as a linguistic error.

# 26. Primary Metrics

For the 60 scored MERGE/KEEP pairs calculate:

1. total;
2. reference MERGE count;
3. reference KEEP count;
4. derived MERGE count;
5. derived KEEP count;
6. derived UNCERTAIN count;
7. overall action agreement;
8. MERGE reference recall;
9. KEEP reference recall;
10. KEEP→MERGE disagreement count/rate;
11. MERGE→KEEP disagreement count/rate;
12. MERGE→UNCERTAIN count;
13. KEEP→UNCERTAIN count;
14. malformed output count;
15. invalid relation count;
16. relation distribution across all seven values.

# 27. Auxiliary UNCERTAIN Review

For reference UNCERTAIN pairs, report returned relations/actions separately.

These do not determine the primary merge/keep agreement percentages.

However:

a reference UNCERTAIN pair deriving MERGE is a safety signal and must be listed prominently.

Do not automatically fail the candidate solely because it differs from an UNCERTAIN reference without Architect review.

# 28. Final Exit Gate

This is a PRODUCT-CANDIDATE gate, not a linguistic-truth claim.

The relation-only candidate qualifies to proceed to v3 implementation if ALL are true:

1. malformed outputs = 0;
2. invalid/unrecognized relations = 0;
3. model does not collapse to one derived action;
4. MERGE reference recall is at least **60%**
   - at least 12 of 20 MERGE-reference pairs derive MERGE;
5. KEEP→MERGE disagreement is no more than **10%**
   - no more than 4 of 40 KEEP-reference pairs derive MERGE;
6. derived UNCERTAIN rate on the 60 primary pairs is no more than **10%**
   - no more than 6 of 60;
7. every KEEP→MERGE disagreement is preserved in a human-review packet;
8. there is no obvious infrastructure/model failure pattern.

These thresholds are intentionally less rigid than treating the project reference as linguistic ground truth.

They are intended to decide whether the candidate is plausible enough for transparent learner-facing product evaluation.

# 29. Hard NO-GO Conditions

Regardless of aggregate score, the candidate is NO-GO if:

- model output is structurally unreliable;
- relation output frequently falls outside the schema;
- derived MERGE behavior is obviously indiscriminate;
- KEEP→MERGE disagreement exceeds 10%;
- MERGE recall is below 60%;
- model collapses to effectively all MERGE or all KEEP;
- more than 10% of primary pairs become UNCERTAIN.

If NO-GO:

STOP automatic Qwen consolidation work for v1.

Do not tune another prompt.

# 30. Human-Review Packet

Create a benchmark artifact containing every:

- KEEP→MERGE disagreement;
- MERGE→KEEP disagreement;
- MERGE→UNCERTAIN result;
- reference UNCERTAIN→MERGE result.

For each show:

- word;
- POS;
- source sense A original glosses;
- source sense B original glosses;
- returned semantic relation;
- derived action;
- project-policy reference action.

Recommended:

`review_packet.md`

and machine-readable:

`review_packet.json`

This exists so Product Owner / Architect can see the actual semantic disagreements rather than relying only on percentages.

# 31. Phase A Outcome

After scoring, declare exactly one:

## `RELATION_ONLY_GATE = GO`

or

## `RELATION_ONLY_GATE = NO_GO`

No ambiguous "maybe."

If thresholds are exactly met, GO.

If any hard gate fails, NO_GO.

# 32. NO-GO Path

If Phase A = NO_GO:

DO NOT modify current production M004.6.13 files.

Do NOT create v3 production code.

Do NOT run additional prompt experiments.

Close M004.6.13.3 with:

**PASS — final exit benchmark completed; relation-only Qwen consolidation NO-GO for v1.**

Recommendation:

- cease automatic sense merging work for this product version;
- preserve source senses separately;
- continue learner wording/ranking/UI work using conservative sense organization;
- retain benchmark and consolidation architecture for later model replacement.

STOP.

# 33. GO Path

Only if:

`RELATION_ONLY_GATE = GO`

proceed to Phase B.

Phase B finalizes the relation-only contract as production candidate v3.

# 34. Production Contract Version

Create:

`PAIR_JUDGMENT_CONTRACT_V3 = "pair-judgment-v3-relation-only-qwen"`

or the exact repository-convention equivalent.

Make v3 the production default.

Preserve:

- v1 historical behavior;
- v2 historical behavior;
- v3 relation-only behavior.

Do not overwrite v1/v2 semantics.

# 35. Model-Facing v3 Contract

Under v3 the model returns only:

{
  "relation": "<one of seven allowed relations>"
}

The provider/application then deterministically derives:

`decision`
+
`reason`

where:

`reason = relation`

and `decision` comes from Section 6.

# 36. Preserve Existing Domain/Persistence Shape

Where practical, preserve the existing downstream contract:

`PairProviderResult`
    ->
decision
+
reason

The model-facing provider may internally receive only `relation`, then map it deterministically into the existing result structure.

Do NOT redesign:

- PostgreSQL tables;
- PairJudgment persisted shape;
- topology;
- complete-link algorithm;
- job model;
- cache model.

No migration is expected.

# 37. Application Validation

The application validator remains authoritative.

After deterministic mapping:

- `merge` may only carry:
  
  - `paraphrase_or_duplicate`
  - `contextual_variant`

- `keep_separate` may only carry:
  
  - `specialized_subsense`
  - `broader_or_narrower`
  - `related_but_distinct`
  - `materially_different`

- `uncertain` may only carry:
  
  - `insufficient_evidence`

Retain validation even though deterministic code constructs the pair.

# 38. Historical Compatibility

Explicitly prove:

- historical v1 PairJudgments remain loadable;
- historical v2 PairJudgments remain loadable;
- v3 uses the full seven-combination application validator;
- no historical row is rewritten.

# 39. Identity Separation

Prove:

same logical pair
+
same evidence
+
v1
!=
v2
!=
v3

Also prove:

same v3 semantic input
=

same v3 judgment identity

across repeated construction.

# 40. Cache Isolation

Using disposable PostgreSQL 17 migration-004 schema, prove:

- existing v1 completed topology is not reused as v3;
- existing v2 completed topology is not reused as v3;
- v3 produces its own compatible job/judgment identity;
- completed v3 replay makes zero additional provider calls.

# 41. No Migration

Do NOT add a database migration unless absolutely unavoidable.

If v3 relation-only implementation appears to require a migration:

STOP and report.

The expected implementation should fit the existing identity/persistence design.

# 42. Provider Tests

If GO, add/update tests for:

1. each of the seven valid relation outputs;
2. deterministic relation→decision mapping;
3. malformed JSON;
4. extra fields;
5. unknown relation;
6. provider timeout/error;
7. truncation;
8. model digest mismatch;
9. mapping cannot produce an invalid decision/reason combination;
10. deterministic request identity.

# 43. Domain Tests

If GO, prove:

- v1 validator compatibility remains historical;
- v2 full allowlist remains historical;
- v3 mapping obeys full allowlist;
- v1/v2/v3 identities differ;
- v3 identity is stable.

# 44. Word-Level Smoke After GO

After v3 implementation, run a small bounded real word-level smoke using the normal M004.6.12 workflow.

Select:

- one word with at least one multi-sense partition;
- one additional word if needed to exercise both MERGE and KEEP behavior.

Before calls:

plan pair counts.

Maximum additional Phase B live calls:

**8 Qwen calls.**

Required proof:

- normal evidence projection;
- relation-only provider;
- deterministic mapping;
- validated pair judgments;
- topology completion when coverage is complete;
- second identical invocation makes zero additional provider calls.

# 45. Total Milestone Live Call Budget

Phase A:

maximum 72.

Phase B, only on GO:

maximum 8.

Absolute milestone maximum:

**80 Qwen calls.**

Do not exceed 80.

# 46. Regression Requirements After GO

If v3 is implemented, run at minimum:

- consolidation-domain tests;
- provider tests;
- orchestrator/adapter tests;
- word-level workflow tests;
- persistence-shape tests;
- normal vocab workflow tests;
- synthesis tests;
- M004.6.11 PostgreSQL tests;
- M004.6.12 PostgreSQL tests.

Known M004.5 candidate-identity test debt remains out of scope.

# 47. Benchmark Artifacts

Persist outside repository:

`/home/chuck/.local/share/vocab-app/benchmarks/m004.6.13.3/<timestamp>/`

At minimum:

- `selection.json`
- `requests.jsonl`
- `responses.jsonl`
- `results.json`
- `results.csv`
- `summary.json`
- `review_packet.json`
- `review_packet.md`

Record SHA256 for:

- selection;
- results;
- summary;
- review packet.

# 48. Benchmark Harness Tests

Add tests proving:

- M004.6.13.2 pair IDs are excluded;
- deterministic 20 MERGE / 40 KEEP selection;
- auxiliary UNCERTAIN selection;
- relation schema contains exactly seven states;
- relation→action mapping is exact;
- KEEP→MERGE disagreement metric is correct;
- MERGE recall metric is correct;
- uncertainty rate is correct;
- GO/NO_GO threshold calculation is correct;
- review packet includes every required disagreement category.

# 49. Product-Evaluation Direction After GO

If v3 passes, do NOT continue semantic benchmark tuning.

The next milestone should move to the learner experience.

The learner UI must make consolidation transparent.

For every generated learner meaning derived from multiple source senses, provide an expandable affordance conceptually like:

**Based on 3 source meanings ▸**

Opening it should expose the original source senses used in the learner container.

At minimum show:

- original Wiktionary/source glosses;
- relevant labels;
- useful examples where available.

Technical IDs may remain in Advanced Details rather than normal learner presentation.

The purpose is to let the Product Owner use the application and judge whether consolidation is actually useful.

# 50. Product Trial Philosophy

If relation-only passes this exit gate, benchmark work is no longer the primary quality signal.

The subsequent product trial should answer:

> Are the generated learner meanings useful, understandable, and generally organized the way a learner would want?

The Product Owner should be able to inspect what was combined whenever something looks questionable.

Flagging remains the simple dissatisfaction mechanism.

Do not require the user to classify the exact semantic failure during normal use.

# 51. Expected Real-Use Evaluation

The intended next-stage Product Owner evaluation is approximately:

**50–100 real word searches**

with normal usage.

Observe:

- good merges;
- over-merging;
- excessive splitting;
- wording quality;
- Core/Additional usefulness;
- flagged entries.

This is not implemented in M004.6.13.3.

It describes why this milestone's gate only needs to establish that relation-only classification is plausibly safe/useful enough to expose transparently in the product.

# 52. No UI Work Here

Do NOT implement:

- expandable source-meaning UI;
- learner wording changes;
- Core/Additional ranking;
- Save/Retry/Refresh changes;
- Flag changes;
- responsive/mobile work.

Those belong to the downstream learner-integration milestone after this semantic exit decision.

# 53. Static Validation

Always run:

`python3 -m py_compile`

on new/changed benchmark files.

Run:

`git diff --check`

If GO and production files change, include all changed production/test Python files in `py_compile`.

# 54. Required Closeout

Create:

`milestones/004.6.13.3_relation_only_semantic_contract_and_final_qwen_consolidation_exit_gate_closeout.md`

Include:

## 1. Status

PASS / PARTIAL / FAIL.

A valid NO-GO outcome may still be:

**PASS — final exit benchmark completed; Qwen automatic consolidation NO-GO for v1.**

## 2. Starting State

HEAD, branch, upstream, worktree.

## 3. Parent Arc State

M004.6.13 / .1 / .2.

## 4. Final Architectural Question

Why relation-only is being tested.

## 5. Reference Dataset Caveat

Explicitly state project-policy reference, not external linguistic ground truth.

## 6. Model/Runtime Identity

Ollama, model, digest, options.

## 7. Relation-Only Prompt

Exact text/hash.

## 8. Relation-Only Schema

Exact schema/hash.

## 9. Deterministic Mapping

All seven relation→action mappings.

## 10. Holdout Construction

M004.6.13.2 exclusions and deterministic selection.

## 11. Frozen Holdout

20 MERGE + 40 KEEP + auxiliary UNCERTAIN IDs.

## 12. Artifact Identity

Paths/hashes.

## 13. Call Accounting

Calls/retries.

## 14. Primary Results

60-pair metrics.

## 15. Relation Distribution

All seven relation counts.

## 16. MERGE Reference Agreement

Counts/rate.

## 17. KEEP Reference Agreement

Counts/rate.

## 18. KEEP→MERGE Disagreements

Literal pairs.

## 19. MERGE→KEEP/UNCERTAIN Disagreements

Literal pairs.

## 20. Auxiliary UNCERTAIN Results

Separate review.

## 21. Review Packet

Path/hash.

## 22. Exit-Gate Evaluation

Each threshold PASS/FAIL.

## 23. Final Relation-Only Disposition

Exactly:

GO

or

NO_GO.

## 24. If NO-GO

Confirm:

- no production v3 implementation;
- no additional prompt tuning;
- recommendation to end Qwen automatic consolidation work for v1.

## 25. If GO — v3 Implementation

Files and exact changes.

## 26. Historical Compatibility

v1/v2/v3 behavior.

## 27. Identity Separation

Proof.

## 28. Persistence/Cache Proof

Disposable PostgreSQL results.

## 29. Word-Level Smoke

Calls/results/cache replay.

## 30. Regression Suite

Commands/results.

## 31. Product-Trial Recommendation

Move to transparent learner UI evaluation rather than further prompt calibration.

## 32. Benchmark Artifact Inventory

Paths/hashes.

## 33. Static Validation

`py_compile`, `git diff --check`.

## 34. Files Changed

Distinguish:

- pre-existing M004.6.13 changes;
- new M004.6.13.3 benchmark files;
- conditional v3 changes.

## 35. Final Git Status

Literal `git status --short`.

## 36. STOP Gate

Product Owner / Architect review.

# 55. Milestone PASS Criteria — Benchmark / Exit Decision

M004.6.13.3 may close PASS when:

1. exactly one relation-only candidate is tested;
2. model is not asked to emit an action;
3. seven semantic relations are closed and explicit;
4. Python mapping is deterministic;
5. M004.6.13.2 pairs are excluded from the primary holdout;
6. holdout selection is deterministic and frozen;
7. exactly 20 MERGE and 40 KEEP pairs are scored unless the reference source cannot support this;
8. auxiliary UNCERTAIN pairs are reported separately;
9. model/options remain locked;
10. no semantic retries occur;
11. call budget is respected;
12. all required metrics are reported;
13. reference limitations are described accurately;
14. review packet is produced;
15. gate results in an explicit GO or NO_GO;
16. no second prompt/schema candidate is introduced;
17. no model/seed tuning is introduced;
18. static validation passes;
19. coder performs no Git writes.

# 56. Additional PASS Criteria If GO

If relation-only = GO, M004.6.13.3 may finalize v3 only when:

1. contract receives a new v3 identity;
2. v1 remains loadable;
3. v2 remains loadable;
4. v3 is distinct/deterministic;
5. provider model output contains only relation;
6. decision is code-derived;
7. application validation remains authoritative;
8. no database migration is required;
9. v3 does not reuse v1/v2 topology incorrectly;
10. cache replay works;
11. bounded live word-level smoke succeeds;
12. relevant regressions pass.

# 57. Final Product Decision Rule

At the end of this milestone:

## If GO

Freeze Qwen consolidation semantics.

Do not do more prompt calibration.

Proceed to transparent learner-facing evaluation.

## If NO-GO

Stop automatic Qwen sense merging for v1.

Do not initiate another prompt-calibration milestone.

Proceed with conservative source-sense handling and continue the learner product.

A future better local model or semantic technique may reopen consolidation as a separate future architecture decision.

# 58. Final Instruction

This is the exit gate.

Do not optimize endlessly.

Test whether Qwen can classify the semantic relationship without also being asked to make the product-policy decision.

If it can do that well enough:

let deterministic code make the action decision and move to real product use.

If it cannot:

stop asking Qwen3:8B to perform automatic sense consolidation and move on with the application.

Either result is an acceptable engineering outcome.

Do not Stage/Commit/Push/Tag/Merge.

Create the required closeout and STOP for Product Owner / Architect review.

**End of M004.6.13.3 prompt**
